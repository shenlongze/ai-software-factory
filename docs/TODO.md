# AI Factory OS · TODO（活清单）

> 这份清单是**活的**：做完一条就更新状态、写上版本号和提交号（可核对 ✓）。
> 每次改动都同时进 `docs/WORKLOG.md`（追加式, 不改历史 ⇒ 留痕 ✓）。
> 网页版：`factory serve` 起来后看 `/status`（由 `scripts/build_status.py` 从这三个来源生成 ✓）。

最后更新：2026-09-22 · 当前版本 `v1.3.26`

---

## 1. 已完成（带痕迹）

| # | 事项 | 版本 | 提交 | 证据 |
|---|---|---|---|---|
| D1 | 看板改成真看板（横排四列 · 中文对齐 · 主题色） | v1.3.3 | `18316e6a` | `factory kanban` 实测 |
| D2 | 看板按项目分屏 + 每列条数可调（`--limit`/`--all`/`--merged`） | v1.3.4 | `10e21f14` | `--limit 2` vs `--all` 短长对比 |
| D3 | 看板表头挂项目中文说明（源 = 会话需求原话） | v1.3.5 | `16e466a1` | `kanban` 表头实拍 |
| D4 | 七域可下钻（`console activity/projects/agents/decisions/cost/experience`） | v1.3.6 | `8f6b3fbc` | 六域都能跑·活动域有真列 |
| D5 | 会话归属项目（`/project <名字｜id｜片段>` · 进提示词 · 可清空） | v1.3.7 | `fd706682` | 假 provider 抓提示词 ✓ |
| D6 | dashboard 与 status 口径归一（projects/tasks） | v1.3.8 | `4d548acb` | 项目 3 · 任务 1000 · 完成 9 == status |
| D7 | 发布交付链：`factory serve` 一条命令起 API + 最小界面 | v1.3.9 | `58368904` | `/` 200 · `/api/trees` 200 |
| D8 | 干净环境真装真跑（抓到并修掉 `index.html` 没进 wheel） | v1.3.10 | `774bd2e5` | 修前 `/` 500 → 修后 200 |
| D9 | 真流式（直连 HTTP + `stream:true`，非终端保持非流式，失败回退） | v1.3.11 | `0a3f416c` | 真接口首块 **0.65s** |
| D10 | 仓库级跨进程运行锁（抢不到拒绝开跑；陈旧锁自动接管） | v1.3.12 | `3beb26e5` | 真子进程抢锁被拒 |
| D11 | 回写防护（patch 目标脏 ⇒ 拒绝自动套用） | v1.3.13 | `64b0e89f` | 脏目标被拒并点名文件 |
| D12 | 架构/分类铁律：修 R20/R23（其一是我漏登记 ✗）+ 存量盘点报告 | v1.3.14 | `e2563a7c` | 分类 3/6 → 5/6 |
| D13 | `create` 手感（company 有回执 · 缺参/乱写中文 + rc=2）+ 修 pydantic 告警 | v1.3.15 | `2c625f1e` | `--root=` 临时根实测 |
| D14 | 解耦安装（独立 venv + wheel，仓库删了也能跑） | v1.3.16 | `2a821e65` | 模块 `__file__` 指向自己的 site-packages |
| D15 | 全视图口径交叉核对（守住 6 个视图，防漂） | v1.3.17 | `c1a7ddc3` | 六视图 == 权威命令 |
| D16 | 学习自治扩 workflow 域 | v1.3.18 | `edf80101` | 收尾钩子 + 失败安全 |
| D17 | 学习自治扩 skill / decision 域 | v1.3.19 | `67538852` | 证据用 Evidence 对象（防静默失效 ✓） |
| D18 | **留痕**：TODO 活清单 + 追加式日志 + `/status` 网页版（真源生成） | v1.3.20 | `43ae62b6` | `/status` 200（干净安装也 200 ✓） |
| D19 | 结构债 **R10**：10 个 `models.py` → `types.py` + 引用三形式全覆盖 | v1.3.21 | `95864dad` | 架构守卫 **R10 绿（0 项）** · pytest 74 ✓ |
| D20 | 结构债 **R4 部分**：纯契约归位 `contracts/llm/` | v1.3.22 | `0def7f76` | R4 违规 **17 → 12** · pytest 74 ✓ |
| D21 | 结构债 **R9**：contracts 控制流外移 `services/resource/rules.py` | v1.3.23 | `592cae04` | R9 违规 **12 → 2** · 真跑建实体 ✓ |
| D22 | **R3 依赖图 + 断环方案**（纯分析 · mermaid 图 · 可重生成） | v1.3.24 | 见下一条提交 | 62 边 · 真环 1 个(6 域) · 双向 8 对 · 契约占比 12% |

## 2. 待办（我能直接做）

| # | 事项 | 说明 |
|---|---|---|
| T2 | 结构债 **R5**（4）：`infrastructure` 越层依赖 | 方向性错误（infra → services/plugins）⇒ 搬类型 / 注入 |
| T5 | 结构债 **R3**（62 边）：断环 | **图已出** ✓ `docs/reports/2026-09-22-跨域依赖与断环方案.md`；真环 1 个（6 域大环 ✗）· 直接双向 8 对 · 像契约仅 12% ⇒ 建议先拿最薄一对试点 ✓ |
| T6 | 结构债 **R11**（86）：`services/<域>/` 五件套布局 | 最重：按域一次一域，每域一提交 + `--help` 对比 |
| T7 | 学习自治 **project 域**落点 | 语义要 Founder 定（树 confirm？chain 跑完？） |

## 3. 待 Founder 拍板

| # | 事项 | 需要你定什么 |
|---|---|---|
| P1 | **6 项待裁决** | R3/R4 处理口径 · 两套 release 并存 · 三个"迁了没删" · heal 是否做插件 · 改名 |
| P2 | 打不打 **tag + Release** | v1.3.4 ~ v1.3.19 这段还没打 tag（你说过先不打 ✓） |
| P3 | **真隔离 worktree**（动底座） | 每任务一个 `git worktree` + 完事合并 ⇒ 要动执行器三处 |
| P4 | **project 域**"项目经验"的时刻 | 里程碑算哪一刻 |

## 4. 明确不做（有理由）

| # | 事项 | 为什么不做 |
|---|---|---|
| N1 | 状态栏"上下文占用条"（Hermes 有 `[████░░] 45%`） | 没有"模型上下文上限"这个**真数据** ⇒ 不编 ✗ |
| N2 | 全屏 `↑/↓` 选择菜单 | 纯 shell 做不到（要 TUI）⇒ 用单键数字/Esc 替代 ✓ |

## 5. 冷启动端到端测试发现（2026-09-22, 详见 `docs/reports/2026-09-22-冷启动端到端测试.md`）

| # | 问题 | 严重度 | 状态 |
|---|---|---|---|
| E1 | **老区命令面仍可触达** ＋ **我在回执里发明了「下一步去招人」**（已撤 ✗）⇒ 真问题是「编排这一环在冷启动没落地」✗ | 严重 | 提示已撤 ✓；编排环待你定方向 |
| E2 | `org employee hire --role devops/architect/writer` **直接崩**（AttributeError, 有 repro）| 严重 | 待修（低风险）|
| E3 | chain 结尾给**假地址**「浏览器: http://127.0.0.1:8787/」（没服务在跑）| 中 | 待修 |
| E4 | 「定位」文案含糊（同环出现「跳过」和「补做」）| 低 | 待修 |
| E5 | 登记表里有不存在的命令（`factory doctor`）⇒ R23 可加「登记必须存在」| 低 | 待修 |
