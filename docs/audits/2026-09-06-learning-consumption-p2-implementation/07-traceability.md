# 07 — TRACEABILITY (P2-D IMPL, 2026-09-06)

RD-* → profile (PROFILE-{agent}-vN) → PROM-* (approval) → CAND-* →
OBS-* → exp-* (anchored) → run/EXS → (release → EVD/ver/art) → task → product

实证: test_trace_full_chain (3 exp 全链) + E2E-1 trace_decision
纯 FK; 每跳 store 可查; 无字符串推断
