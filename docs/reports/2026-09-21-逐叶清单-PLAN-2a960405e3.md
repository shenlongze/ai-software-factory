# 树 PLAN-2a960405e3 逐叶清单（83 叶）

## 模块 1: 基础设施与脚手架


### 初始化微信小程序与后端服务仓库结构，配置开发/测试环境、环境变量管理、CI 基础流水线与微信小程序 AppID/密钥配置

1. **初始化小程序与后端服务仓库结构**（需要: architect/devops/developer）
   - 验收: 仓库包含 miniprogram 与 server 两个目录，均有基础入口文件和 README，目录结构约定文档化
2. **配置环境变量模板与统一读取机制**（需要: architect/devops/developer）
   - 验收: 提供各服务的 .env.example，含 ENV_API_BASE_URL、WX_APPID、WX_SECRET、JWT_SECRET，任一服务能读到这些变量并有默认值兜底
3. **搭建开发与测试环境配置**（需要: architect/devops/developer）
   - 验收: 存在 development/test 两套配置，按 NODE_ENV 切换加载，启动后打印当前环境并可被测试用例读取
4. **配置微信小程序 AppID 与密钥接入**（需要: architect/devops/developer）
   - 验收: 小程序端配置好 AppID，后端具备用 WX_APPID/WX_SECRET 调用微信接口换取凭证的具体代码，并且可以在测试环境通过 mock 走通
5. **搭建 CI 基础流水线**（需要: architect/devops/developer）
   - 验收: 提交代码后 CI 自动执行安装依赖、lint 与单测三步，全绿则通过，失败能定位到具体步骤

## 模块 2: 认证与角色权限


### 实现微信小程序登录（code2Session）、用户注册/绑定、JWT 签发与校验、基于角色（会员/教练/店长/前台）的权限中间件

6. **实现微信 code2Session 登录接口并签发 JWT**（需要: developer/security）
   - 验收: POST /auth/wx-login {code} 能返回 {token, user:{id,role,nickname,phone}}，token 可被后续接口校验通过
7. **实现用户注册与自动绑定逻辑**（需要: developer/security）
   - 验收: 首次登录的微信用户自动创建用户记录并绑定 openid，再次登录复用同一用户，数据库可查到该记录
8. **实现 GET /auth/me 返回当前用户信息**（需要: developer/security）
   - 验收: 携带合法 Bearer token 请求 /auth/me 返回 {id,role,profile}，无 token 或非法 token 返回 401
9. **实现 POST /auth/bind-phone 手机号绑定接口**（需要: developer/security）
   - 验收: POST /auth/bind-phone {phone,code} 校验验证码成功后更新用户手机号并返回 {user}，验证码错误返回 400
10. **实现 JWT 校验中间件**（需要: developer/security）
   - 验收: 受保护接口在 Header 缺少或携带非法 Authorization: Bearer <token> 时返回 401，合法 token 时能解析出用户身份
11. **实现基于角色的权限中间件**（需要: developer/security）
   - 验收: 会员/教练/店长/前台四种角色访问越权接口返回 403，授权角色访问返回正常响应，且有单测覆盖各角色

## 模块 3: 领域模型与数据存储


### 设计并落地核心数据表：用户、教练资料、可授课时间段、课程预约、课时包与课时账户、消课记录、通知记录，编写 ORM 模型与迁移脚本

12. **设计核心数据表并编写用户/教练资料表ORM模型与迁移**（需要: architect/developer）
   - 验收: 用户表与教练资料表建成，含 id/created_at/updated_at，可插入一条用户并通过外键关联到教练资料
13. **编写可授课时间段表ORM模型与迁移**（需要: architect/developer）
   - 验收: 可授课时间段表建成，可插入一条关联到教练资料的时间段记录并成功查询
14. **编写课时包与课时账户表ORM模型与迁移**（需要: architect/developer）
   - 验收: 课时包表与课时账户表建成，balance 单位为次，可创建课时包并为用户开通账户并写入初始余额
15. **编写课程预约表ORM模型与迁移**（需要: architect/developer）
   - 验收: 课程预约表建成，含 course_session_id 字段，可插入一条关联用户与可授课时间段的预约记录
16. **编写消课记录表ORM模型与迁移**（需要: architect/developer）
   - 验收: 消课记录表建成，通过 course_session_id 关联预约，可插入一条消课记录并正确扣减课时账户 balance
17. **编写通知记录表ORM模型与迁移**（需要: architect/developer）
   - 验收: 通知记录表建成，可插入一条关联用户的 通知 记录并成功查询
18. **编写统一迁移脚本与回滚脚本并验证全部表结构**（需要: architect/developer）
   - 验收: 执行迁移可在空库建出全部表，执行回滚可全部删除，重复执行幂等无报错

## 模块 4: 教练可授课时间管理


### 实现教练设置/编辑/删除可授课时间段的接口与日历展示，校验时间段冲突

19. **实现查询教练可授课时间段接口 GET /coach/availability**（需要: developer/ui-designer）
   - 验收: 调用接口传 coachId 与 from/to 范围，能返回符合格式的可用时间段列表，字段包含 id/weekday/startTime/endTime/status
20. **实现新增可授课时间段接口 POST /coach/availability**（需要: developer/ui-designer）
   - 验收: 提交 weekday/startTime/endTime 后创建成功并返回新 id，数据可被查询接口读到
21. **实现编辑可授课时间段接口 PUT /coach/availability/{id}**（需要: developer/ui-designer）
   - 验收: 修改已存在时间段的字段后返回对应 id，查询结果同步更新
22. **实现删除可授课时间段接口 DELETE /coach/availability/{id}**（需要: developer/ui-designer）
   - 验收: 删除已存在时间段返回 success 为 true，该记录不再出现在查询结果中
23. **实现时间段冲突校验逻辑**（需要: developer/ui-designer）
   - 验收: 新增或编辑与同教练已有时间段在相同 weekday 且时间区间重叠时返回冲突错误，不重叠时正常保存
24. **实现教练可授课时间段日历展示界面**（需要: developer/ui-designer）
   - 验收: 日历按 weekday 与时间轴渲染各时间段及其状态，数据来自查询接口并能随增删改刷新

## 模块 5: 预约与排课


### 实现会员查看教练可约时间、提交预约、确认/取消/改约，以及教练端预约列表与变更通知触发

25. **实现会员查询教练可约时间段接口 GET /coach/{coachId}/slots**（需要: developer/ui-designer）
   - 验收: 调用 GET /coach/{coachId}/slots?date=YYYY-MM-DD 返回数组，每项含 slotId、startTime、endTime、available 字段，且仅返回该教练该日期的时间段
26. **实现会员提交预约接口 POST /appointments**（需要: developer/ui-designer）
   - 验收: 调用 POST /appointments 传入 coachId、slotId、courseType 返回 appointmentId 和 status，成功写入预约记录并占用该 slot
27. **实现预约列表查询接口 GET /appointments（按角色/状态/时间过滤）**（需要: developer/ui-designer）
   - 验收: 调用 GET /appointments?role&status&from&to 返回数组，每项含 id、memberName、coachName、startTime、status，且过滤条件生效
28. **实现预约取消接口 PUT /appointments/{id}/cancel**（需要: developer/ui-designer）
   - 验收: 调用 PUT /appointments/{id}/cancel 传入 reason 返回 status，预约状态更新为已取消且释放对应 slot
29. **实现预约改约接口 PUT /appointments/{id}/reschedule**（需要: developer/ui-designer）
   - 验收: 调用改约接口传入新 slotId 返回 status，原 slot 释放、新 slot 占用，预约时间更新

#### 教练端预约列表页面开发（展示会员名、时间、状态）

30. **设计教练端预约列表页面视觉稿**（需要: developer/ui-designer）
   - 验收: 输出包含会员名、时间、状态字段的预约列表页面设计稿，经确认可用
31. **对接 GET /appointments 接口并处理数据**（需要: developer/ui-designer）
   - 验收: 调用接口返回预约数据并能映射出 memberName、startTime、status 字段
32. **开发教练端预约列表页面并展示三项字段**（需要: developer/ui-designer）
   - 验收: 教练登录后可查看本人预约列表，正确展示 memberName、startTime、status
33. **实现预约变更通知触发（提交/确认/取消/改约时发送通知）**（需要: developer/ui-designer）
   - 验收: 任一预约状态变更操作后，系统向相关会员和教练生成通知记录，可通过日志或通知表验证触发成功

## 模块 6: 签到与消课


### 实现课程签到（扫码/前台确认）、消课确认、课时自动扣减与异常处理（爽约、补课、手动调整课时）

34. **设计签到与消课数据模型**（需要: developer/ui-designer）
   - 验收: 产出 members/checkin/consume_record 等表结构设计文档或迁移文件，能支持签到记录、消课记录、课时调整记录及余额计算字段
35. **实现课程签到接口 POST /checkin**（需要: developer/ui-designer）
   - 验收: 调用 POST /checkin {appointmentId,method} 成功返回 {checkinId,checkedAt}，重复签到或无效 appointmentId 返回明确错误
36. **实现消课确认接口 POST /consume**（需要: developer/ui-designer）
   - 验收: 调用 POST /consume {appointmentId,operatorNote} 生成消课记录并返回 {consumeRecordId,remainingBalance}，余额同步扣减且记录可查
37. **实现手动调整课时接口 POST /consume/adjust**（需要: developer/ui-designer）
   - 验收: 调用 POST /consume/adjust {appointmentId,delta,reason} 生成调整记录并返回 {consumeRecordId,remainingBalance}，正负 delta 均能正确计算余额
38. **实现会员课时余额查询 GET /members/{memberId}/balance**（需要: developer/ui-designer）
   - 验收: 调用 GET /members/{memberId}/balance 返回 {balance,records:[...]}，records 按时间倒序包含签到、消课、调整全部变动记录
39. **处理爽约与补课异常逻辑**（需要: developer/ui-designer）
   - 验收: 爽约可标记并按规则扣减或不扣课时，补课可关联原预约并正确计入余额与记录，均有对应测试用例验证
40. **实现签到与消课前端界面**（需要: developer/ui-designer）
   - 验收: UI 完成后可扫码/前台确认签到、确认消课、手动调整课时并实时展示余额与记录，交互与接口联调通过

## 模块 7: 课时包与余额管理


### 实现课时包字典与后台录入课时包、会员课时账户初始化与余额变更流水记录


#### 实现课时包数据表与字典增查接口（POST/GET /packages）

41. **设计并创建课时包数据表**（需要: developer/ui-designer）
   - 验收: 数据库中课时包表建好，含 id、name、totalSessions、price 字段，字段类型正确且能插入一条测试数据
42. **实现 POST /packages 创建课时包接口**（需要: developer/ui-designer）
   - 验收: 传入 name、totalSessions、price 能创建成功并返回 packageId，数据写入数据库
43. **实现 GET /packages 课时包列表查询接口**（需要: developer/ui-designer）
   - 验收: 返回包含 id、name、totalSessions、price 的列表，且能查到刚创建的记录

#### 实现会员课时账户表、初始化接口与余额查询接口

44. **设计并创建会员课时账户数据表**（需要: developer/ui-designer）
   - 验收: 数据库中已建好会员课时账户表，含 memberId、packageId、grantedBy、note、balance、expireAt 等字段，能成功插入一条测试记录并查出
45. **实现初始化课时账户接口 POST /members/{memberId}/packages**（需要: developer/ui-designer）
   - 验收: 传入 packageId、grantedBy、note 调用该接口，能创建账户并返回 memberPackageId 和 balance，且 balance 等于对应课时包的 totalSessions
46. **实现会员课时账户余额查询接口 GET /members/{memberId}/packages**（需要: developer/ui-designer）
   - 验收: 调用该接口能返回该会员所有课时账户的 memberPackageId、balance、expireAt 列表

#### 实现会员课时余额变更流水记录与查询

47. **建会员课时余额变更流水数据表与初始化写入**（需要: developer/ui-designer）
   - 验收: 账户初始化时能写入一条含变更前余额、变更后余额、变更人、备注、时间的流水记录，可通过数据库查询到该记录
48. **开发余额变更流水查询接口**（需要: developer/ui-designer）
   - 验收: 调用接口按会员ID或账户ID能返回对应流水列表，字段完整且金额与账户当前余额一致
49. **开发流水查询前端页面**（需要: developer/ui-designer）
   - 验收: 页面可按会员或账户筛选并展示流水列表，字段显示完整，金额与账户余额一致

#### 实现后台课时包录入页面并调用 packages 接口

50. **实现课时包录入表单页面与字段校验**（需要: developer/ui-designer）
   - 验收: 页面包含 name、totalSessions、price 三个输入项，必填与数字>0 校验生效并给出错误提示
51. **对接 packages 接口提交课时包并提示成功**（需要: developer/ui-designer）
   - 验收: 点击提交后调用 packages 接口，成功后展示成功提示
52. **提交成功后刷新课时包列表并展示新增项**（需要: developer/ui-designer）
   - 验收: 提交成功后列表自动刷新，新增的课时包出现在列表中
53. **实现后台会员课时账户发放页面并调用发放接口**（需要: developer/ui-designer）
   - 验收: 页面能选择会员和课时包、填写 grantedBy 与 note 后提交，成功后提示发放成功并展示该会员当前账户余额
54. **实现后台会员课时账户与流水明细展示页面**（需要: developer/ui-designer）
   - 验收: 页面能按会员查询并列出其账户（memberPackageId,balance,expireAt）及对应余额变更流水明细，数据与接口返回一致

## 模块 8: 通知与提醒


### 接入微信服务通知与订阅消息，封装预约确认、取消/改约、上课提醒、消课完成等消息模板发送与失败重试

55. **接入微信 access_token 获取与 sendSubscribeMessage 基础发送能力**（需要: developer/devops）
   - 验收: 调用内部函数 sendSubscribeMessage(templateId, openid, data) 能真实发出订阅消息并返回 {errcode}，errcode=0 表示成功，可用真实 openid 验证一条
56. **实现预约确认与取消/改约消息模板发送**（需要: developer/devops）
   - 验收: 订阅事件 APPOINTMENT_CONFIRMED 与 APPOINTMENT_CANCELLED 能触发对应模板消息发送，且用真实 openid 各验证成功一条
57. **实现上课提醒与消课完成消息模板发送**（需要: developer/devops）
   - 验收: 订阅事件 LESSON_REMINDER 与 CONSUME_SUCCESS 能触发对应模板消息发送，且用真实 openid 各验证成功一条

#### 建 notify_log 表记录发送结果与失败重试机制

58. **建 notify_log 数据表并定义字段结构**（需要: developer/devops）
   - 验收: notify_log 表已建好，包含 templateId、openid、errcode、状态、重试次数字段，能成功插入并查询一条记录
59. **发送时写入 notify_log 记录发送结果**（需要: developer/devops）
   - 验收: 每次发送调用后 notify_log 中新增一条记录，含 templateId、openid、errcode、状态，可通过查询验证
60. **实现失败记录重试任务并更新状态**（需要: developer/devops）
   - 验收: 能演示一条失败记录被重试任务重新发送并更新为空成功状态，重试次数递增，notify_log 可查到该状态变更

## 模块 9: 教练工作台与统计


### 实现教练查看个人排课日历、课时统计（已上/待上/取消）与收入概览

61. **实现教练课程日历接口 GET /coach/schedule**（需要: developer/ui-designer）
   - 验收: 调用 /coach/schedule?from&to 返回指定区间内 [{appointmentId,memberName,startTime,status}] 列表，数据与数据库一致且区间过滤正确
62. **实现教练课时统计与收入概览接口 GET /coach/dashboard**（需要: developer/ui-designer）
   - 验收: 调用 /coach/dashboard?from&to 返回 {sessionsDone,sessionsUpcoming,sessionsCancelled,incomeEstimate}，且各项数值与数据库统计一致
63. **设计并实现教练个人排课日历视图**（需要: developer/ui-designer）
   - 验收: 页面按日历展示当前教练课程，取 /coach/schedule 数据，可切换日期区间并区分课程状态
64. **设计并实现课时统计与收入概览面板**（需要: developer/ui-designer）
   - 验收: 页面展示已上/待上/取消课时数与收入估算，取 /coach/dashboard 数据且随区间切换更新

## 模块 10: 店长管理后台


### 实现店长/前台查看排课视图、消课记录、教练与会员业绩基础数据，以及数据导出与基础报表接口

65. **实现排课视图接口 GET /admin/schedule**（需要: developer/ui-designer）
   - 验收: 调用 GET /admin/schedule?date&coachId 返回 [{appointmentId,memberName,coachName,startTime,status}]，按日期筛选正确且有数据返回
66. **实现消课记录接口 GET /admin/consumption**（需要: developer/ui-designer）
   - 验收: 调用 GET /admin/consumption?from&to&coachId 返回 [{consumeRecordId,memberName,coachName,sessions,amount}]，时间区间和教练筛选正确
67. **实现报表汇总接口 GET /admin/report/summary**（需要: developer/ui-designer）
   - 验收: 调用 GET /admin/report/summary?from&to 返回 {totalSessions,totalMembers,coachRank:[...]}，统计数值与源数据一致
68. **实现数据导出接口 GET /admin/export**（需要: developer/ui-designer）
   - 验收: 调用 GET /admin/export?type&from 能按类型和时间导出对应数据文件，内容与查询结果一致
69. **店长/前台排课视图与消课记录页面**（需要: developer/ui-designer）
   - 验收: 页面能按日期/教练筛选展示排课与消课数据，数据来自对应接口且展示正确
70. **报表汇总与导出操作页面**（需要: developer/ui-designer）
   - 验收: 页面能展示报表汇总数据并触发导出下载，数据与接口一致

## 模块 11: 会员中心


### 实现会员查看我的预约、剩余课时、消课记录与个人信息

71. **实现获取我的预约列表接口 GET /me/appointments**（需要: developer/ui-designer）
   - 验收: 传入 status/from/to 可筛选，返回 appointmentId、coachName、startTime、status 字段的列表
72. **实现获取剩余课时及消课记录接口 GET /members/{memberId}/balance**（需要: developer/ui-designer）
   - 验收: 返回 balance 与 records 数组，records 含消课明细，数据与会员账户一致
73. **实现获取个人信息接口 GET /me/profile**（需要: developer/ui-designer）
   - 验收: 返回 nickname、phone、remainingSessions 三个字段，值与当前登录用户一致
74. **设计并实现我的预约页面UI**（需要: developer/ui-designer）
   - 验收: 展示预约列表并支持按状态/时间筛选，可正常调用预约接口渲染
75. **设计并实现剩余课时与消课记录页面UI**（需要: developer/ui-designer）
   - 验收: 展示剩余课时数值与消课记录列表，可正常调用余额接口渲染
76. **设计并实现个人信息页面UI**（需要: developer/ui-designer）
   - 验收: 展示昵称、手机号、剩余课时，可正常调用个人信息接口渲染

## 模块 12: 端到端测试与验收


### 搭建基于真实业务流的端到端测试：会员约课→教练确认→签到→消课扣减→通知发送→店长报表可查，覆盖异常分支（取消、改约、爽约、手动调整课时）

77. **搭建端到端测试框架与测试环境**（需要: tester/developer）
   - 验收: 框架可运行，能通过测试账号登录并调用已有预约接口返回成功
78. **实现正流程端到端测试：约课到报表全链路**（需要: tester/developer）
   - 验收: 测试通过，断言课时余额扣减正确、消课记录生成、通知日志写入、店长报表可查
79. **实现取消约课分支测试并校验课时与通知回退**（需要: tester/developer）
   - 验收: 测试通过，断言取消后课时已回退、消课记录撤销、发送取消通知日志
80. **实现改约分支测试并校验课时与记录一致性**（需要: tester/developer）
   - 验收: 测试通过，断言改约后旧记录更新、新预约生成、课时余额不变、通知日志正确
81. **实现爽约分支测试并校验消课扣减与通知**（需要: tester/developer）
   - 验收: 测试通过，断言爽约后课时被扣减、消课记录标记爽约、发送爽约通知日志
82. **实现手动调整课时分支测试并校验报表一致**（需要: tester/developer）
   - 验收: 测试通过，断言手动调整后课时余额变更、调整记录生成、报表数据同步一致
83. **实现Mock微信OpenAPI的通知发送桩**（需要: tester/developer）
   - 验收: Mock服务可拦截通知调用并记录日志，测试能断言通知内容与目标用户