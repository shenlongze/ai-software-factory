# 树 PLAN-ba224ba1ca 逐叶清单（198 叶）

## 微信小程序登录与角色权限体系


### 模块 1: 认证与角色权限

1. **后端接入微信 code2session 接口**（需要: developer/security）
   - 验收: 传入有效 code 返回 openid 与 session_key，传入无效 code 返回 401 及错误码
2. **小程序端封装 wx.login 调用与 code 上报**（需要: developer）
   - 验收: 点击授权按钮可完成 wx.login 并将 code 发送至后端，成功进入首页、失败展示错误提示
3. **会话 token 生成与下发**（需要: developer/security）
   - 验收: 登录成功后返回带过期时间的 token，token 可被后续接口用于鉴权
4. **小程序端本地存储与携带 token**（需要: developer）
   - 验收: 已登录状态下任意受保护接口请求头自动携带有效 token

### 模块 2: 认证与角色权限

5. **创建 users 表（含 openid/role/nickname/avatar）**（需要: developer）
   - 验收: users 表创建完成，openid 唯一索引生效，role 字段枚举值 member/coach/manager 可写入
6. **首次登录用户自动注册记录**（需要: developer）
   - 验收: 新 openid 首次登录自动创建 users 记录并返回 isNewUser=true，再次登录返回 false
7. **角色字段默认值与后台可修改**（需要: developer）
   - 验收: 管理员可将指定用户 role 修改为 member/coach/manager，且该用户下次请求鉴权时生效

### 模块 3: 认证与角色权限

8. **后端 token 校验中间件**（需要: developer/security）
   - 验收: 无 token 或无效 token 请求受保护接口返回 401，有效 token 放行
9. **token 续期接口**（需要: developer）
   - 验收: 未过期 token 调用续期接口返回新 token，过期 token 返回 401
10. **小程序端 401 自动跳转登录**（需要: developer）
   - 验收: token 失效时前端自动清除本地凭证并跳转登录页

### 模块 4: 认证与角色权限

11. **后端角色鉴权中间件**（需要: developer/security）
   - 验收: 会员访问教练专属接口返回 403，教练访问店长专属接口返回 403，正确角色放行
12. **路由按角色声明可访问列表**（需要: developer）
   - 验收: 每个受保护路由配置允许角色数组，未在列表中的角色一律 403
13. **小程序端按角色渲染功能入口**（需要: developer/ui-designer）
   - 验收: 登录后首页按当前 role 展示对应功能入口，切换测试账号可见性正确

## 模块 2: 用户与教练档案

14. **定义会员实体数据模型（字段与约束）**（需要: architect）
   - 验收: 产出会员表结构定义（含id、微信openid、昵称、手机号、头像、状态、创建时间），字段类型与唯一约束明确。
15. **定义教练实体数据模型（字段与约束）**（需要: architect）
   - 验收: 产出教练表结构定义（含id、关联用户id、姓名、简介、资质、状态、所属门店id），字段类型与外键明确。
16. **定义门店实体数据模型（字段与约束）**（需要: architect）
   - 验收: 产出门店表结构定义（含id、名称、地址、营业状态、创建时间），字段类型明确。
17. **定义角色枚举与权限映射规则**（需要: architect）
   - 验收: 产出角色枚举（member/coach/manager）及每个角色可访问操作的映射表，经评审确认。
18. **编写会员表迁移脚本**（需要: developer）
   - 验收: 执行迁移后会员表存在且字段与领域模型一致，回滚可删除该表。
19. **编写教练表迁移脚本**（需要: developer）
   - 验收: 执行迁移后教练表存在且字段与领域模型一致，回滚可删除该表。
20. **编写门店表迁移脚本**（需要: developer）
   - 验收: 执行迁移后门店表存在且字段与领域模型一致，回滚可删除该表。
21. **实现查询会员详情接口**（需要: developer）
   - 验收: 给定存在的会员id返回200及完整字段，不存在的id返回404。
22. **实现更新会员资料接口**（需要: developer）
   - 验收: 提交合法字段后数据库对应记录被更新且返回更新后对象，非法字段返回400。
23. **实现查询教练详情接口**（需要: developer）
   - 验收: 存在的教练id返回200及完整字段，不存在返回404。
24. **实现更新教练资料接口**（需要: developer）
   - 验收: 提交合法字段后记录被更新并返回新对象，非法字段返回400。
25. **实现按门店列出教练接口**（需要: developer）
   - 验收: 给定storeId返回该门店所有教练列表，无教练时返回空数组。
26. **实现查询门店详情接口**（需要: developer）
   - 验收: 存在的门店id返回200及完整字段，不存在返回404。
27. **实现门店列表接口**（需要: developer）
   - 验收: 返回全部启用门店列表，空库返回空数组。
28. **角色与权限校验中间件**（需要: developer/security）
   - 验收: 中间件能根据请求用户角色拦截无权访问并返回403。
29. **档案模块端到端集成测试**（需要: tester）
   - 验收: 集成测试覆盖会员/教练/门店查询更新及权限拦截场景并全部通过。

## 模块 3: 可授课时间与排课

30. **设计 coach_availability（教练可授课时间段）表结构**（需要: architect）
   - 验收: 表结构含教练ID、星期/日期、起止时间、时长粒度、生效区间、状态字段并通过评审
31. **设计 coach_slot（可约时段）表结构**（需要: architect）
   - 验收: 表结构含教练ID、开始时间、结束时间、是否可约状态、来源时间段ID字段并通过评审
32. **设计 schedule（排课/已预约课程）表结构**（需要: architect）
   - 验收: 表结构含时段ID、会员ID、课程状态、确认时间、取消原因字段并通过评审
33. **设计时段唯一性与防重复约束（同教练同时段不可重复）**（需要: architect）
   - 验收: 给出唯一索引/约束定义，能阻止同教练重叠时段写入并通过评审
34. **实现创建可授课时间段接口**（需要: developer）
   - 验收: 调用接口传入合法时间段后数据库新增一条可授课时间段记录并返回其ID
35. **实现查询教练可授课时间段接口**（需要: developer）
   - 验收: 调用接口返回该教练全部生效时间段且不包含其他教练数据
36. **实现删除/停用可授课时间段接口**（需要: developer）
   - 验收: 删除后该时间段不再出现在查询结果中且已产生的可约时段处理符合约定
37. **实现时间段重叠与合法性校验（起止时间、重叠冲突）**（需要: developer）
   - 验收: 提交与已有时间段重叠的请求时接口返回 409 且不写入数据
38. **实现按可授课时间段切分为固定时长可约时段的排课生成逻辑**（需要: developer）
   - 验收: 给定一条 09:00-12:00 且粒度为60分钟的时间段，生成 09:00/10:00/11:00 三个可约时段
39. **实现可约日历查询接口（按日期区间返回可时段位）**（需要: developer）
   - 验收: 查询指定日期区间返回该教练每个可约时段及其可约状态，已预约时段 bookable=false
40. **实现可授课时间变更后的已生成时段同步更新逻辑**（需要: developer）
   - 验收: 修改可授课时间段后，对应未来未预约时段随之更新且已预约时段保持不变
41. **实现已预约时段不可重复占用校验**（需要: developer）
   - 验收: 对已 bookable=false 的时段再次占用时接口返回 409 且不产生重复排课记录
42. **实现可授课时间设置页面（星期/起止时间/生效区间表单）**（需要: developer/ui-designer）
   - 验收: 教练可在页面填写并提交一条可授课时间段，提交成功后表单重置
43. **实现已设置可授课时间段列表展示与删除操作**（需要: developer）
   - 验收: 列表正确展示已设置时间段，点击删除后该条从列表消失
44. **实现时间段冲突错误提示交互**（需要: developer）
   - 验收: 提交重叠时间段时页面显示明确冲突提示且不跳转
45. **实现可约日历组件（按日/周展示可时段）**（需要: developer/ui-designer）
   - 验收: 日历正确渲染指定区间内可约与已预约时段，置灰状态与接口数据一致
46. **实现日历空状态与加载/错误状态展示**（需要: developer）
   - 验收: 无数据、加载中、请求失败三种状态均有明确可见的界面反馈
47. **实现教练排课列表查询接口**（需要: developer）
   - 验收: 查询返回该教练指定区间内全部排课记录且字段完整
48. **实现教练课时统计接口（已排课/已消课数量）**（需要: developer）
   - 验收: 统计接口返回指定区间内已排课数与已消课数且与排课明细条数一致
49. **编写可授课时间设置到可约日历生成的端到端测试**（需要: tester）
   - 验收: 测试用例覆盖设置时间段后日历出现对应可约时段并自动化通过
50. **编写时段冲突与重复占用校验的测试用例**（需要: tester）
   - 验收: 测试覆盖重叠时间段创建返回409、已占用时段再次占用返回409并通过
51. **编写排课与课时统计一致性测试**（需要: tester）
   - 验收: 测试校验统计接口数值与排课明细记录数一致并通过

## 模块 4: 预约管理

52. **定义预约状态枚举与合法迁移规则**（需要: architect）
   - 验收: 枚举含 pending/confirmed/cancelled/rescheduled/completed/no_show，且对每对状态明确是否允许，形成迁移矩阵
53. **设计预约表结构（含状态字段与时间戳）**（需要: architect）
   - 验收: 预约表含 id/member_id/coach_id/slot_id/status/created_at/updated_at 及索引，DDL 可执行无报错
54. **设计预约状态流转历史表**（需要: architect）
   - 验收: 状态流转表含 appointment_id/from_status/to_status/operator_id/created_at，且能还原任一预约的状态变更链路
55. **预约创建接口与时段占用校验**（需要: developer）
   - 验收: 对同一 slot_id 并发两次创建仅一次成功，另一次返回时段已占用错误码
56. **会员剩余课时校验**（需要: developer）
   - 验收: 剩余课时为 0 时创建预约返回课时不足错误码且不落库
57. **会员端预约确认弹窗与提交**（需要: developer/ui-designer）
   - 验收: 点击确认预约后页面出现新预约卡片，状态显示『待确认』
58. **预约确认接口与权限校验**（需要: developer/security）
   - 验收: 仅该预约的教练或店长可确认，越权返回 403
59. **确认后触发微信服务通知**（需要: developer）
   - 验收: 确认成功后会员收到一条含课程时间与教练姓名的微信服务通知
60. **教练端待确认列表与确认按钮**（需要: developer）
   - 验收: 教练点击确认后列表实时更新，无需手动刷新
61. **预约取消接口与时段释放**（需要: developer）
   - 验收: 取消成功后该 slot_id 重新出现在可约列表，且其他会员可再次预约
62. **取消时限规则校验**（需要: developer）
   - 验收: 距开课不足配置阈值的预约调用取消接口返回超时错误码且状态不变
63. **取消原因记录与展示**（需要: developer）
   - 验收: 取消后详情接口返回 cancel_reason，页面展示与提交内容一致
64. **取消后通知对方角色**（需要: developer）
   - 验收: 会员取消后教练收到通知，教练取消后会员收到通知，各一条
65. **改约接口：原单作废与新单创建（事务）**（需要: developer）
   - 验收: 改约在单事务内完成，新时段被占用失败时原预约状态保持不变
66. **改约次数限制校验**（需要: developer）
   - 验收: 达到配置改约上限后再次改约返回错误码且不产生新预约
67. **改约新时段选择页复用可约列表**（需要: developer）
   - 验收: 选中可约时段并点击确认改约后页面跳回详情并显示新时间
68. **改约结果通知双方**（需要: developer）
   - 验收: 改约成功后会员与教练各收到一条含新旧时间的通知
69. **状态流转统一写入服务（禁止绕过）**（需要: developer/reviewer）
   - 验收: 所有状态变更经统一方法写入，代码审查确认无直接 UPDATE status 的其它路径
70. **变更历史查询接口**（需要: developer）
   - 验收: 历史接口返回记录条数与状态流转表实际行数一致
71. **详情页变更记录时间轴组件**（需要: developer/ui-designer）
   - 验收: 页面时间轴节点顺序与接口返回顺序一致且文案可读
72. **按角色过滤（会员看自己/教练看自己/店长看全部）**（需要: developer/security）
   - 验收: 会员 token 调用仅返回本人预约，教练 token 仅返回本人课程，越权数据不出现在结果中
73. **按状态与时间范围过滤与分页**（需要: developer）
   - 验收: 组合筛选返回条数与 total 一致且分页边界正确
74. **预约列表页与状态 Tab**（需要: developer）
   - 验收: 切换 Tab 后列表内容随之变化且加载更多可用
75. **预约操作权限中间件**（需要: developer/security）
   - 验收: 对 confirm/cancel/reschedule 三类操作接入权限校验，越权用例测试全部 403
76. **同教练时段冲突校验**（需要: developer）
   - 验收: 同一教练同一时段已有未取消预约时，再次创建返回 409
77. **会员同一时段重复预约校验**（需要: developer）
   - 验收: 同一会员对同一时段重复提交返回 409 且仅存在一条预约
78. **创建→确认→取消 链路测试**（需要: tester）
   - 验收: 测试断言每一步状态与时段占用/释放均符合预期
79. **创建→改约→确认 链路测试**（需要: tester）
   - 验收: 测试断言原单为 rescheduled、新单为 confirmed 且原时段已释放
80. **并发预约与改约竞态测试**（需要: tester）
   - 验收: 并发 10 次抢同一时段，仅 1 次成功且无脏数据

## 课时账户与消课模块整体设计与接口契约定义


### 模块 1: 课时账户与消课

81. **设计课时包模板表（credit_package_template）**（需要: developer）
   - 验收: DDL 含 id、name、total_credits、valid_days、price、status、created_at 字段并成功在测试库建表
82. **设计会员课时账户表（member_credit_account）**（需要: developer）
   - 验收: DDL 含 id、member_id、package_template_id、total_credits、remaining_credits、expired_at、status 字段且 member_id 建唯一索引
83. **设计课时流水表（credit_transaction）**（需要: developer）
   - 验收: DDL 含 id、account_id、type(recharge/consume/refund/adjust)、delta、balance_after、ref_id、operator_id、created_at 字段并建 account_id+created_at 联合索引
84. **设计消课记录表（consume_record）**（需要: developer）
   - 验收: DDL 含 id、booking_id、member_id、coach_id、consume_status、consumed_at、operator_id、remark 字段并建 booking_id 唯一索引防重复消课

### 模块 2: 课时账户与消课

85. **实现创建课时包模板接口**（需要: developer）
   - 验收: 调用接口后数据库新增一条 status=active 的课时包模板记录并返回其 id
86. **实现课时包模板列表查询接口**（需要: developer）
   - 验收: 接口返回当前所有 active 状态课时包模板，字段与 UI 展示一一对应
87. **实现课时包模板停用接口**（需要: developer）
   - 验收: 调用后目标模板 status 变为 inactive，后续创建会员账户不可选该模板
88. **为会员后台录入课时包（开通账户）接口**（需要: developer）
   - 验收: 调用后新增会员课时账户，remaining_credits 等于模板 total_credits，并写入一条 type=recharge 的课时流水

### 模块 3: 课时账户与消课

89. **实现会员剩余课时查询接口**（需要: developer）
   - 验收: 接口返回当前登录会员的有效账户剩余课时，与流水表 balance_after 最新值一致
90. **实现课时流水明细查询接口**（需要: developer）
   - 验收: 分页返回该会员所有课时流水，按 created_at 倒序，条数与 total 一致
91. **实现店长查询指定会员课时余额接口**（需要: developer）
   - 验收: 店长查询任意会员均返回其账户余额，无账户时返回 404 并附明确错误码
92. **实现课时余额校验的领域服务方法**（需要: developer）
   - 验收: 提供 canConsume(memberId) 同步方法，当剩余课时<=0 或账户已过期时返回 false，单元测试覆盖两种边界

### 模块 4: 课时账户与消课

93. **实现消课事务服务（扣课时+写流水+写消课记录）**（需要: developer）
   - 验收: 单事务内完成扣减 remaining_credits、写入 credit_transaction(consume) 与 consume_record，任一失败回滚，集成测试验证
94. **实现重复消课幂等校验**（需要: developer）
   - 验收: 同一 booking_id 第二次调用消课返回 409 且不产生新流水，集成测试验证
95. **实现教练签到消课接口**（需要: developer）
   - 验收: 调用后课程状态变为 consumed，会员剩余课时减 1，返回新余额
96. **实现消课记录列表查询接口**（需要: developer）
   - 验收: 按筛选条件返回消课记录，分页正确，时间范围过滤准确
97. **实现会员查看个人消课记录接口**（需要: developer）
   - 验收: 接口仅返回当前会员的消课记录，且教练名与 bookig 关联正确

### 模块 5: 课时账户与消课

98. **实现店长手动调整课时接口**（需要: developer）
   - 验收: 调用后余额按 delta 变化，生成 type=adjust 流水，reason 必填否则 400
99. **实现爽约扣课时处理接口**（需要: developer）
   - 验收: 调用后按规则扣减 1 课时并写 type=consume 且 remark=no_show 的流水
100. **实现补课不扣课时的豁免逻辑**（需要: developer）
   - 验收: 调用后生成 make-up 类型消课记录但不新增扣课时流水，余额不变

### 模块 6: 课时账户与消课

101. **本模块接口角色权限校验**（需要: developer/security）
   - 验收: 会员访问 /api/admin/* 返回 403；非本人教练消课返回 403；通过接口级权限测试用例
102. **从录入课时包到签到消课全链路端到端测试**（需要: tester）
   - 验收: 自动化用例覆盖：创建模板→开户→预约→签到消课→查询余额与记录，全部断言通过

## 模块 6: 异常处理

103. **定义爽约异常数据模型（goNoShowRecord：会员/教练/课程/爽约时间/判定依据/状态）**（需要: architect）
   - 验收: 字段清单与类型在评审中逐字段确认，与课程、用户模型的外键关系明确
104. **定义补课记录数据模型（makeupLesson：原课程/新课程/关联会员/教练/补课状态）**（需要: architect）
   - 验收: 字段清单与类型在评审中逐字段确认，原课程与新课程的外键关系明确
105. **定义手动课时调整数据模型（manualCreditAdjustment：会员/调整数量/原因/操作人/审计时间）**（需要: architect）
   - 验收: 字段清单与类型在评审中逐字段确认，必含操作人与原因字段
106. **定义三类异常的状态枚举与合法状态迁移表**（需要: architect）
   - 验收: 状态迁移表以表格形式列出所有合法迁移，非法迁移在评审中标记为拒绝
107. **爽约判定规则引擎（到点未签到自动标记爽约）**（需要: developer）
   - 验收: 课程开始后超过配置分钟数未签到，该课程订单自动生成爽约记录且状态为待确认
108. **教练/店长手动标记爽约接口**（需要: developer）
   - 验收: 教练或店长在课程详情页点击标记爽约并填写原因后，系统生成一条状态为已确认的爽约记录
109. **爽约扣课时逻辑（按规则扣减会员剩余课时）**（需要: developer）
   - 验收: 爽约确认后会员剩余课时按规则扣减且生成一条消课流水，余额与流水一致

### 爽约申诉与撤销接口

110. **实现 POST /api/no-show/{noShowId}/appeal 提交申诉接口**（需要: developer）
   - 验收: 调用该接口传入 reason 后返回 {status}，且该爽约记录状态变为申诉中
111. **实现 POST /api/no-show/{noShowId}/revoke 店长撤销爽约接口**（需要: developer）
   - 验收: 调用该接口返回 {status, refundedCredits}，且课时已返还、原爽约记录状态变为已撤销
112. **爽约记录列表查询接口（按会员/教练/时间筛选）**（需要: developer）
   - 验收: 传入筛选条件后返回匹配的爽约记录列表且分页总数与数据一致
113. **创建补课申请接口（关联原课程与新时间）**（需要: developer）
   - 验收: 会员选择新时间段提交后生成一条状态为待确认的补课记录并关联原课程

### 补课审批接口（教练或店长确认/拒绝）

114. **实现补课审批通过接口（POST /api/makeup/{makeupId}/approve）**（需要: developer）
   - 验收: 调用后接口返回成功，补课状态变为已确认，且新课程出现在排课列表中
115. **实现补课审批拒绝接口（POST /api/makeup/{makeupId}/reject）**（需要: developer）
   - 验收: 调用后接口返回成功，补课状态变为已拒绝，且拒绝原因被持久化记录
116. **补课不重复扣课时校验逻辑**（需要: developer）
   - 验收: 补课确认后会员剩余课时与补课前一致，且不产生额外消课流水
117. **补课与原课程的关联展示接口**（需要: developer）
   - 验收: 查询补课详情时同时返回原课程与补课课程信息且两者关联字段一致
118. **补课记录列表查询接口**（需要: developer）
   - 验收: 传入筛选条件后返回匹配的补课记录列表且分页总数与数据一致
119. **手动调整课时接口（增/减/设置）**（需要: developer）
   - 验收: 店长提交调整后会员剩余课时按调整值变化且生成一条调整记录含操作人与原因
120. **手动调整权限校验（仅店长/管理员）**（需要: developer/security）
   - 验收: 教练或会员调用调整接口返回 403，仅店长及以上角色可成功调整
121. **调整原因与备注必填校验**（需要: developer）
   - 验收: 未填写原因提交时返回 400 且提示原因必填，填写后可成功提交
122. **调整记录审计日志写入**（需要: developer/security）
   - 验收: 每次调整后审计日志包含操作人、时间、前后余额与原因，且只能追加不可修改
123. **调整记录列表查询接口**（需要: developer）
   - 验收: 传入筛选条件后返回匹配的调整记录列表且分页总数与数据一致
124. **爽约确认后向会员发送微信服务通知**（需要: developer）
   - 验收: 爽约确认后会员收到含课程名与扣课时数的微信服务通知，发送记录可在日志中查到
125. **补课确认后向会员与教练发送微信服务通知**（需要: developer）
   - 验收: 补课确认后会员与教练各收到含新时间地点的微信服务通知
126. **手动调整课后向会员发送课时变动通知**（需要: developer）
   - 验收: 调整后会员收到含调整数量与当前余额的微信服务通知
127. **爽约全流程端到端测试（自动标记→扣课时→通知→申诉→撤销返还）**（需要: tester）
   - 验收: 脚本跑完爽约全流程每一步断言通过，含撤销后课时返还断言
128. **补课全流程端到端测试（申请→审批→不重复扣课时→通知）**（需要: tester）
   - 验收: 脚本跑完补课全流程每一步断言通过，含课时不重复扣减断言
129. **手动调整全流程端到端测试（权限→调整→审计→通知）**（需要: tester）
   - 验收: 脚本跑完手动调整全流程每一步断言通过，含无权限拒绝与审计记录断言

## 模块 7: 通知提醒

130. **定义消息类型与事件枚举**（需要: architect/product-manager）
   - 验收: 输出消息类型枚举清单（预约成功/改约/取消/上课前提醒/消课确认等），含每类的触发事件、渠道、优先级，评审通过
131. **定义通知数据模型与状态机**（需要: architect）
   - 验收: 输出通知记录表结构（含状态：待发送/已发送/失败/已读）与状态流转图，评审通过
132. **定义发送链路与幂等重试策略**（需要: architect）
   - 验收: 输出发送链路时序图与幂等键规则、重试次数与退避策略，评审通过
133. **梳理并申请订阅消息模板**（需要: product-manager/developer）
   - 验收: 输出模板清单（模板ID、标题、字段key），且每个模板已在小程序后台成功申请
134. **编写模板字段映射配置**（需要: developer）
   - 验收: 配置文件中每个模板的字段key与业务数据字段一一对应，且通过单元测试校验无缺失
135. **创建通知记录表迁移脚本**（需要: developer）
   - 验收: 迁移脚本可在空库执行成功并在回滚后无残留，表结构与设计文档一致
136. **实现通知记录模型 CRUD**（需要: developer）
   - 验收: 通知记录新增/查询/状态更新接口的单元测试全部通过
137. **实现用户订阅关系表与读写**（需要: developer）
   - 验收: 订阅关系表迁移成功，且记录用户对每个模板的订阅状态可读写并有单元测试覆盖
138. **实现请求订阅授权弹窗交互**（需要: developer/ui-designer）
   - 验收: 点击按钮后微信订阅弹窗出现，用户选择结果被回调捕获并有日志记录
139. **上报订阅授权结果到后端**（需要: developer）
   - 验收: 授权结果写入订阅关系表，接口单元测试覆盖 accepted=true/false 两种情况
140. **后端保存订阅状态逻辑**（需要: developer）
   - 验收: 同一用户同一模板重复上报时以最新状态为准，数据库唯一约束生效，单元测试通过
141. **封装微信订阅消息发送客户端**（需要: developer）
   - 验收: 对微信接口的调用有超时、错误码解析，且用 mock 服务端可单元测试通过
142. **实现 access_token 获取与缓存**（需要: developer）
   - 验收: token 在有效期内复用、过期自动刷新，且有并发安全与过期刷新单元测试
143. **实现发送前订阅状态校验**（需要: developer）
   - 验收: 未订阅用户调用 canSend 返回 false，已订阅返回 true，单元测试覆盖
144. **实现发送幂等键处理**（需要: developer）
   - 验收: 相同 idempotencyKey 重复调用只产生一条通知记录，单元测试通过
145. **实现发送结果落库与状态更新**（需要: developer）
   - 验收: 发送成功后通知记录状态为已发送，失败为失败并记录错误码，单元测试覆盖成功与失败两路
146. **预约成功事件触发通知**（需要: developer）
   - 验收: 创建预约后通知记录表新增一条预约成功通知，集成测试通过
147. **预约改约事件触发通知**（需要: developer）
   - 验收: 改约成功后通知记录表新增一条改约通知，集成测试通过
148. **预约取消事件触发通知**（需要: developer）
   - 验收: 取消预约后通知记录表新增一条取消通知，集成测试通过
149. **消课确认事件触发通知**（需要: developer）
   - 验收: 消课确认后通知记录表新增一条消课通知，集成测试通过
150. **实现定时扫描待提醒预约任务**（需要: developer/devops）
   - 验收: 定时任务按调度周期运行，扫描结果日志可见，单元测试覆盖时间窗口边界
151. **实现提醒幂等（同预约不重复提醒）**（需要: developer）
   - 验收: 同一预约重复扫描只发送一次提醒，单元测试通过
152. **实现提醒任务调度注册**（需要: developer/devops）
   - 验收: 调度配置在部署环境生效，任务异常有告警，部署后日志可见首次执行
153. **实现失败重试队列**（需要: developer）
   - 验收: 失败通知按配置的间隔与次数被重试，单元测试覆盖重试成功与耗尽两路
154. **实现超限失败告警**（需要: developer/devops）
   - 验收: 重试耗尽的通知触发告警日志或通知，且可在日志中检索到对应记录
155. **通知记录查询接口与页面**（需要: developer/ui-designer）
   - 验收: 会员在『我的通知』页能看到自己的历史通知列表并正确分页，接口与页面联调通过
156. **通知提醒端到端测试**（需要: tester）
   - 验收: 从预约创建到用户收到订阅消息的全链路端到端测试用例通过

## 模块 8: 统计与报表

157. **课时统计领域模型设计**（需要: architect）
   - 验收: 产出课时统计领域模型文档，定义统计维度（教练、时间范围、课程状态）与聚合字段，评审通过
158. **教练课时统计查询 API**（需要: developer）
   - 验收: 调用接口传入教练ID和日期范围，返回该教练课时总数、已完成数、取消数、总时长，数据与消课记录一致
159. **教练收入概览查询 API**（需要: developer）
   - 验收: 调用接口返回该教练指定时间段收入总额、课次数、单课均收入，金额与课时单价×消课数一致
160. **教练课时统计页面**（需要: developer/ui-designer）
   - 验收: 教练端打开统计页可切换日期范围，页面展示课时与收入数据且与接口返回一致
161. **店长业绩数据模型设计**（需要: architect）
   - 验收: 产出业绩数据模型文档，定义按门店、教练、时间维度的消课与收入聚合口径，评审通过
162. **门店业绩概览 API**（需要: developer）
   - 验收: 调用接口返回门店时间段内总课时数、总收入、活跃教练数、活跃会员数，数据与消课记录聚合一致
163. **教练业绩排行 API**（需要: developer）
   - 验收: 调用接口返回按课时数降序的教练排行列表，每项含教练名、课时数、收入，数据与各教练统计一致
164. **店长业绩看板页面**（需要: developer/ui-designer）
   - 验收: 店长打开业绩页选择日期后可看到门店概览与教练排行，数据与接口一致
165. **导出接口 - 课时明细导出**（需要: developer）
   - 验收: 调用接口下载CSV，内容含每节课的日期、教练、会员、状态、时长，行数与消课记录一致
166. **导出接口 - 业绩汇总导出**（需要: developer）
   - 验收: 调用接口下载CSV，内容含教练维度课时数与收入汇总，与排行接口数据一致
167. **导出按钮与下载交互**（需要: developer）
   - 验收: 点击导出选择类型后浏览器下载对应CSV文件，内容与页面数据一致
168. **统计接口角色鉴权**（需要: developer/security）
   - 验收: 教练仅能查询本人统计，店长仅能查询本门店数据，越权请求返回403
169. **导出接口鉴权与范围校验**（需要: developer/security）
   - 验收: 非店长角色调用导出接口返回403，店长导出范围限制在本门店

## 模块 9: 管理后台

170. **初始化后台前端项目与依赖**（需要: developer）
   - 验收: 执行启动命令后本地可打开空白后台首页且无控制台报错。
171. **实现后台主布局与侧边菜单**（需要: developer）
   - 验收: 登录后可见左侧菜单与顶部信息，点击菜单可切换对应占位页面。
172. **实现店长角色路由守卫**（需要: developer）
   - 验收: 用非店长 token 访问 /schedule 时被拦截并提示无权限。
173. **实现统一请求封装与错误提示**（需要: developer）
   - 验收: 调用一个受保护接口且 token 失效时，页面自动跳回登录页并给出提示。
174. **实现登录页表单与验证码倒计时**（需要: developer/ui-designer）
   - 验收: 输入合法手机号点击获取验证码后按钮进入 60 秒倒计时且不可重复点击。
175. **实现登录接口对接与 token 存储**（需要: developer）
   - 验收: 店长登录后 token 被写入本地存储且跳转到后台首页。
176. **实现退出登录**（需要: developer）
   - 验收: 点击退出登录后本地 token 被清除且回到登录页。
177. **实现概览卡片组件**（需要: developer/ui-designer）
   - 验收: 接口返回后四个卡片分别显示对应数值且加载态正确切换。
178. **实现日期切换查看历史概览**（需要: developer）
   - 验收: 选择昨天后卡片数据刷新为昨天对应数值。
179. **实现排课列表表格渲染**（需要: developer）
   - 验收: 接口返回 N 条数据时表格渲染 N 行且状态标签颜色与状态对应。
180. **实现按日期与教练筛选**（需要: developer）
   - 验收: 选择某教练后列表仅显示该教练的排课记录。
181. **实现排课分页加载**（需要: developer）
   - 验收: 翻到第二页时展示接下来的记录且总条数与接口 total 一致。
182. **实现新增排课表单弹窗**（需要: developer/ui-designer）
   - 验收: 填写完整并提交后弹窗关闭且列表出现新排课记录。
183. **实现排课冲突错误提示**（需要: developer）
   - 验收: 提交与已有课程重叠的时间段时弹窗保持打开并显示冲突提示。
184. **实现取消确认弹窗与原因输入**（需要: developer/ui-designer）
   - 验收: 未填写原因时确认按钮为禁用状态，填写后可点击提交。
185. **实现取消成功后的列表刷新**（需要: developer）
   - 验收: 取消成功后列表自动刷新且该记录状态显示为已取消。
186. **实现消课列表表格与筛选**（需要: developer）
   - 验收: 按日期筛选后列表仅展示该日期内的消课记录。
187. **实现手动消课按钮与确认弹窗**（需要: developer/ui-designer）
   - 验收: 点击手动消课弹出确认框且展示正确的会员与课程信息。
188. **实现消课成功后状态与课时刷新**（需要: developer）
   - 验收: 消课成功后列表状态更新为已消课且弹窗或 toast 显示剩余课时数。
189. **实现时间范围选择与查询**（需要: developer）
   - 验收: 切换时间范围后业绩数据按新范围刷新且与接口返回一致。
190. **实现汇总指标卡片**（需要: developer）
   - 验收: 汇总卡片数值与接口返回的 totalRevenue/totalLessons 一致。
191. **实现按教练业绩明细表格**（需要: developer）
   - 验收: 表格按 coachBreakdown 渲染且点击业绩列表头可切换排序。
192. **实现课时包列表与会员搜索**（需要: developer）
   - 验收: 输入会员手机号搜索后列表仅展示该会员的课时包记录。
193. **实现后台录入课时包**（需要: developer）
   - 验收: 录入后新课时包出现在列表中且剩余课时等于录入的总课时。
194. **实现手动调整课时表单与接口对接**（需要: developer）
   - 验收: 提交一次调整后接口返回新的剩余课时且与会员课时页显示一致。
195. **实现爽约状态标记**（需要: developer）
   - 验收: 点击标记爽约并确认后该排课状态显示为爽约。
196. **实现导出表单与下载触发**（需要: developer）
   - 验收: 点击导出按钮后浏览器下载到与所选类型对应的 CSV 文件。
197. **实现空结果导出提示**（需要: developer）
   - 验收: 导出无数据的时间范围时页面提示无数据且不下载空文件。
198. **后台整体端到端验收**（需要: tester/reviewer）
   - 验收: 店长可完整走通登录→查看首页概览→排课管理→手动消课→查看业绩→导出 CSV 全流程且无阻断性报错。