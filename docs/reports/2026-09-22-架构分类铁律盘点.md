# 架构 / 分类铁律存量红盘点（2026-09-22）

> 只读盘点。**没有动任何业务代码** ✗ —— 大头（R3/R11 等）属于结构重构, 按分刀纪律留给 Founder 裁决。
> 载体: `scripts/check_architecture.py`（R1–R17）· `scripts/check_classification.py`（R18–R23）

## 一、现状（真跑读数）

| 组 | 通过 | 红 | 红项（条数） |
|---|---|---|---|
| 架构 R1–R17 | 9/15 | 6 | R3 跨域 import（60）· R11 `services/<域>/ ⊆ 五件套`（86）· R4 plugins 只许 import contracts（17）· R9 contracts 不许含控制流（12）· R10 禁用文件名（10）· R5 infrastructure 只许 import contracts（4） |
| 分类 R18–R23 | 3/6 | 3 | R18 一能力一域（8）· R20 清单单一（1）· R23 命令登记（1） |

跨层/跨域 import 边 **213 条**。

## 二、这一刀修掉的两个（便宜且该修 ✓）

| 规则 | 红项 | 处置 | 证据 |
|---|---|---|---|
| **R23** 命令登记 | 10 个顶层命令未登记（`chain` `discover` `help` `knowledge` `llm` `mcp` `memory` `serve` `start` `tool`） | 全部登记进 `apps/cli/registry.py`（按功能语义判域）；★ 其中 **`serve` 是我新增发布命令时漏登记** ✗ | `check_classification.py` ⇒ R23 转绿 |
| **R20** 清单单一 | `contracts/entity` 目录在、SSoT 清单无 | 补进 `docs/ssot/architecture.md` §二 契约域（12 → 13） | `check_classification.py` ⇒ R20 转绿 |

## 三、留待裁决的大头（我的建议）

| 规则 | 红 | 性质 | 我的建议 |
|---|---|---|---|
| R11 五件套布局 | 86 | 目录结构重排（`services/<域>/` 应为 service/store/rules/events/contracts） | **缓**: 纯结构, 改动面大、收益是"合规"不是"能用"；建议**按域一次一域**做（每域一 commit, `--help` 对比证明命令面不变） |
| R3 跨域 import | 60 | 60 条跨域依赖 | **先测后拆**: 先给 60 条画出依赖图（谁依赖谁、有没有环）, 再按域断环（我们自己记过: 按域拆分后跨域环仍在 ⇒ 断环要用注入 ✓） |
| R4 plugins 只许 import contracts | 17 | 插件直接 import 了基础设施（如 `events.types`、`llm.provider`） | **中**: 多为类型引用 ⇒ 可把类型搬到 `contracts/`（低风险） |
| R9 contracts 不许含控制流 | 12 | `contracts/entity/contract.py` 里有 12 处 `if` | **中**: 校验逻辑应落到 services/rules ⇒ 契约只留数据 |
| R10 禁用文件名 | 10 | 禁用的文件名（如 `utils.py` 类） | **快**: 逐个改名 + 引用同步（一刀可清） |
| R5 infrastructure 只许 import contracts | 4 | `llm/providers/integration.py` → `services/execution/*` · `storage/loader.py` → `plugins/factories/*` | **快**: 4 条, 走注入/搬类型 |
| R18 一能力一域 | 8 | 同一能力出现在多个服务域 | **要你先定域**: 到底归哪个域 —— 属于业务裁决, 我不自己拍 ✓ |

## 四、结论（一句话）

- 这一刀: **R23 + R20 转绿**（其中 R23 有我的漏登记 ✗ 已补）
- 剩下的 **7 条**都是**结构债, 不是 bug** ⇒ 不影响"能用", 但影响"像不像一个严格管理的产品" ✓
- 建议顺序: **R10 / R5 / R4 / R9**（快、低风险）⇒ 再 **R3 断环**（要图 + 注入）⇒ 最后 **R11 按域重排**（最重）
- R18 需要你先拍"哪个能力归哪个域" ✓

