# S10-068 Part 2 — Autonomous Debug & Repair Intelligence

> 日期:2026-08-17 | Sprint: S10-068 Part 2 | Autonomous Debug & Repair
> 状态: 从"提出修复建议"→"受治理约束的自主 Debug / Repair"

---

## 1. 完整闭环

```
FAILURE → ERROR UNDERSTANDING → ROOT CAUSE → MEMORY RETRIEVAL → FIX STRATEGY
  → REPAIR → VALIDATION → PASS → 成功
                    ↓ FAIL
                 再分析 → 换策略 (adaptation) → 再 Repair
                    ↓ 连续失败
                 LoopGuard/Budget → BLOCKED / WAITING_FOR_REVIEW
  → Memory Learning (经验沉淀) → Audit-ready Trace
```

## 2. 新增能力

| 能力 | 说明 |
|---|---|
| DebugSession/DebugAttempt | 9 状态机 (ANALYZING→…→SUCCESS/BLOCKED/REVIEW) + 持久化 debug_sessions.json |
| StrategyAdapter | 排除已失败策略 + 替代策略 + 全败→REQUEST_REVIEW; strategy_history 记录 |
| RepairSafety | Governance 约束: AUTO/SAFE_AUTO/REVIEW/BLOCKED (Budget/LoopGuard/Policy/ReviewGate) |
| DebugRetrievalPolicy | 查询构建/来源选择/Top-K/重排(项目>confidence>新鲜>成功)/去重 + 未来多 RAG 扩展点 |
| ContextBudget | token 预算: candidates/selected/discarded/estimated_tokens/max_tokens/latency |
| DebugTrace | Audit-ready: debug_id/strategy/governance/cost/tokens/latency → debug_trace.json |
| RootCause 9 类 | CODE_DEFECT/TEST_DEFECT/REQUIREMENT_MISMATCH/ENVIRONMENT/DEPENDENCY/CONFIGURATION/DATA/INTEGRATION/UNKNOWN + reasoning_summary |
| DebugPipeline | 完整闭环: start/analyze/repair/validate/adapt/run/resume/learn (CLI→Core→API 同一入口) |

## 3. Capability Delivery

```
Core:
✅ DebugSession + Store + StrategyAdapter + RepairSafety + RetrievalPolicy + ContextBudget + DebugTrace + DebugPipeline + RootCause 9 类

CLI:
✅ debug_session    — "开始调试"/"调试会话"
✅ debug_root_cause — "找一下根因"
✅ debug_repair     — "自动修复"
✅ debug_validate   — "验证修复"
✅ debug_resume     — "继续调试"
(+ 已有 debug_analyze/history/recommend/stats)
-h: ✅ 9 命令全注册 + 5 新意图关键词

API:
✅ POST /api/debug/session    — 开始会话
✅ POST /api/debug/root-cause — 根因
✅ POST /api/debug/repair     — 修复
✅ POST /api/debug/validate   — 验证
✅ POST /api/debug/resume     — 继续
(+ 已有 analyze/recommend/history/stats)
schema: ✅ Pydantic + error handling; 注册: ✅ api/__init__.py (9 端点全注册)

Tests:
✅ Core 43 + CLI/API/E2E 28 = 71 新测试 (五覆盖: Core/CLI/API/Governance/E2E)
```

## 4. 真实 E2E 证据 (ScorePocket Memory + 完整闭环)

```
CASE A: TEST_FAILURE → FIX_CODE → 修复 → SUCCESS ✅
CASE B: FIX_CODE 失败 → adaptation → FIX_TEST (换策略) ✅
CASE C: 预算耗尽 → BLOCKED (Governance 约束生效, 不无限烧钱) ✅
CASE D: LLM 不可用 → NULL → FIX_CODE (deterministic 安全) ✅
DebugTrace: 落盘 (debug_id 完整) ✅
```

## 5. 测试

```
新增: 71 (Core 43 + CLI/API/E2E 28)
全量: 11388 passed + 1 skipped, 0 failed (11317 基线 → +71, 零回归)
```

## 6. 技术债

- run() 自动闭环验证器需真实测试执行集成 (Part 2 用注入 validator)
- Repair 执行用 RepairManager 桥 (execute_fn 注入), 未直接接真实 Agent 修改
- 多 RAG 来源仅接口预留 (select_sources 可扩展)
- LLM Debug Reasoning 为可选 (deterministic 为主)

## 7. 下一 Sprint 建议

```
S10-069 — Audit Intelligence (Who/What/When/Why/Impact/Approval 企业级审计)
  DebugTrace/learning_trace/governance 记录已就位, 可审计
```

---

> S10-068 Part 2 文档完毕 | Autonomous Debug & Repair | 71 新测试 | 11388 全绿
