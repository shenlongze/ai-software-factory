# S10-006.5 — P1-A Report: Workflow 启动 + 真实 Agent 执行链（Completion Report）

> 日期: 2026-08-10 | 状态: 完成 (待人工审核) | pytest 6594 + vitest 267 + tsc 零错

## 交付内容

```
1. 端点:
   POST /api/projects/{id}/start   — 启动真实 Agent 执行链 (后台线程, 立即返回 started)
   POST /api/projects/{id}/chat    — 持续开发对话 (未启动→idea 更新+触发 start; 已启动→recorded)
   GET  /api/projects/{id}/run-status — 运行状态 + 进度 (轮询驱动, 成本/调用数实时可见)

2. 执行链 (workflow_runner.py):
   真实链: pm→uxui→architect→developer→tester→release (DeepSeek v4-pro)
   后台线程执行 (不阻塞 HTTP) | 事件写 events store (Timeline 同源)
   进度 report.json/progress.json (stages/totals/errors/updated_at)
   失败安全: key 缺失 503 / 重复启动 409 / 项目不存在 404 / 空消息 400
   测试注入: chain_factory (假链零 LLM)

3. 前端:
   api client: startWorkflow/sendChat/runStatus
   (项目工作台"开始开发"按钮 + Timeline chat 接线由后续 UI 任务接入 — 本任务 API 层已通)
```

## 真实冒烟 (Web 端点 → 全链 v4-pro 真实执行)

```
POST /api/projects "开发一个记账 App" → ledger-app (201)
POST /start → {status: started, run_id}

轮询 run-status (真实执行进度):
  [running] stages=[product]         calls=1  tokens=2,366  cost=$0.00095   (PM 真实需求分析)
  [running] stages=[product, ux_ui]  calls=3  tokens=16,832 cost=$0.00646  (UX/UI 设计)
  [running] stages=[+design]         calls=4  tokens=28,273 cost=$0.01057  (Architect)
  [running] stages=[+development, testing] calls=6 tokens=68,262 cost=$0.02570
  [completed] stages=[product, ux_ui, design, development, testing, release]
             calls=7  tokens=69,728  cost=$0.02625  wall=822s (~14min)

验证:
  GET /timeline → 30 个真实事件 (artifact 生成/验证通过/阶段开始/完成/工作流完成)
  GET /workflow → WF completed (development→testing→release 链)
```

## 修复的真实问题 (冒烟中发现)

```
1. baseline commit failed (空仓库): git 中文环境输出 "无文件要提交",
   代码只匹配英文 "nothing to commit" → 误判失败 → 兼容中文
2. report.json 缺 finished_at: run-status updated_at 为 None → 补写
```

## 测试

```
后端 +15 (test_console_s10_workflow_start.py: start 200/404/409/503 + chat 200/400
  + run-status none/running/completed + 事件可读 timeline 聚合 + 路由函数语义)
共 pytest 6594 (6579+15) + vitest 267 + tsc 零错
```

## 用户旅程达成

```
你输入 "开发一个记账 App" → 创建 ledger-app → 点开始开发 (或发消息触发)
→ PM Agent 真实需求分析 → UX/UI 设计 → 架构 → 编码 → 测试 → 发布
→ Timeline 实时可见每个阶段/产物 (真实, 非 mock)
```

## 下一步

```
1. 前端 UI 接线: "开始开发"按钮 + Timeline chat 输入 (API 已就绪)
2. Review 门集成: 真实 gate 出现在审核中心
3. Release 下载: zip 产物下载端点
```
