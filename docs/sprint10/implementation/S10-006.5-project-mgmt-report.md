# S10-006.5 — 项目管理（Completion Report）

> 日期: 2026-08-10 | 状态: 完成 (待人工审核) | pytest 6602 + vitest 284 + tsc 零错

## 交付内容

```
1. 后端端点:
   PATCH /api/projects/{id}  {name?, idea?} → 200 更新摘要 / 400 空字段 / 404
   DELETE /api/projects/{id} → 200 {deleted: true} / 404 / 409 (workflow 运行中拒绝)
   org: ProjectStore.delete_project (原子写幂等) + ProjectLifecycle.delete_project
   service: update_project (model_copy 落库) + delete_project (运行中保护 + 清理 runs/chat)
   (既有项目数据经 delete 前二次确认 + 运行中 409 诚实保护)

2. 前端:
   Home 已有项目列表 + 项目树: 每项目 ⋯ 菜单 (重命名/删除)
   重命名 Modal: 预填当前名 → PATCH → 列表/树/Header 全同步 (后端回显名)
   删除 Modal: 二次确认 "删除后不可恢复" → DELETE → 移除; 选中项删除自动回 Home
   409 运行中 → "项目正在开发中, 无法删除" (Modal 停留可取消)
   client: updateProject (PATCH) / deleteProject (DELETE)
```

## 测试

```
后端 +8 (TestProjectManagementEndpoints: 重命名 200/改 idea/空 name 400/空 body 400/404/
  删除 200/404/运行中 409) + 白名单更新 (PATCH/DELETE 仅限 /api/projects)
前端 +12 (project-mgmt.test.tsx: 菜单/重命名 PATCH+同步/空名拦截/删除确认/DELETE/409 提示)
  + client 4 测试
共 pytest 6602 (6594+8) + vitest 284 (268+16) + tsc 零错
```

## 实测 (真实环境)

```
PATCH /api/projects/P-dbfeb38f {name: "记账本"}
  → {project_id, name: 记账本, idea: 开发一个记账 App, status: idea} ✅
GET /api/projects → 列表显示 "记账本" ✅
DELETE /api/projects/P-NOPE → 404 ✅
截图: /tmp/s10-project-mgmt/project-mgmt.png
```

## 下一步

```
1. 前端"开始开发"按钮 (run-status none → 显示; 已接 API)
2. Timeline chat 输入 UI 接线 (POST /chat 已就绪)
3. Review 门集成 (真实 gate 进审核中心)
```
