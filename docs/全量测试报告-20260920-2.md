# 全量测试报告（第二次）

日期: 2026-09-20 · 数据根: `/Users/agentdev/.factory` · 场景: 健身房私教排课消课小程序（`~/factory-scenarios/gym-coach`, 项目 `P-019cc935`, 计划 `PLAN-9e32c8b904`）
执行方式: 逐项真跑, 读数照抄; 未测项单列在 §11。

---

## 1. 结论摘要

```
 工具健康        全绿（ruff 全仓 0 错 · 导入 318/318 · 守卫自检 7/7 + 8/8 · shell 语法 ✓）
 验证守卫        裸 pytest 23 passed · verify.sh 16/16 · 六个守卫扫真实根全过
 项目理解能力    ✓ 通（阶段识别 + 完整报告, 体现实证据）
 项目级记忆      ✓ 通（存储/写入/注入/第 3 层 四段都实测；写侧已接真事件）
 8 环链路        7 环有真实读数, 执行环正在跑第 4 个叶
 存量债          架构 R1–R17 9/15 条通过（有红）· 分类 R18–R22 3/6 条通过（有红）
 本次新发现      4 项（见 §10）—— 其中 1 项是"理解产物落盘会过期"
```

## 2. 工具健康（A 段）

| 项 | 命令 | 读数 |
|---|---|---|
| 裸 pytest（含全部守卫） | `.venv/bin/python -m pytest -q` | **23 passed** |
| canonical gate | `bash scripts/verify.sh` | **工具健康 16 项通过 ✓** |
| ruff 全仓 | `ruff check apps src scripts` | **All checks passed**（今天从 47 债清到 0） |
| 全量导入 | `scripts/check_imports.py -q` | **318/318 通过, 全绿** |
| 架构守卫自检 | `scripts/check_architecture.py --selftest` | **7/7 通过** |
| 分类守卫自检 | `scripts/check_classification.py --selftest` | **8/8 通过** |
| shell 语法 | `bash -n scripts/*.sh` | **全部通过** |

存量债（③ 段，不阻塞；`--strict` 可阻塞）:

```
 架构 R1–R17    9/15 条规则通过（有红）· 跨层/跨域 import 边 210 条
 分类 R18–R22   3/6 条规则通过（有红）—— Founder: "代码必须严格分类，严格管理，不能东一个西一个"
```

## 3. 六个守卫扫【真实数据根】（B 段）

| 守卫 | 读数 |
|---|---|
| `smoke_user_view --root ~/.factory` | 24/24 通过 |
| `smoke_priority --root` | 2/2 通过 |
| `smoke_keypath --root` | 2/2 通过 |
| `smoke_exec_dispatch --root` | 2/2 通过 |
| `smoke_staffing --root` | 2/2 通过 |
| `smoke_chain_links --root` | 12/12 通过 |

## 4. 项目理解能力（C 段，真跑）

**C1 阶段识别**（`factory understand <项目路径> --stage`）:

```
 ✔ 阶段识别: OPERATION  (confidence: 0.90)
    证据 4 条 artifact（每条带文件名清单）:
      · SOURCE_CODE   apps/api/src/auth/auth.controller.ts 等 8 个
      · TEST          apps/api/test/auth/*.spec.ts 等 8 个
      · DEPLOYMENT    docker-compose.yml
      · OPERATION     .github/workflows/ci.yml
```

**C2 完整报告**:

```
 阶段 OPERATION (0.90) · 类型 service · 规模 medium (83 files / 23 dirs) · 状态 operational
 语言 javascript, typescript · 技术栈 node
 缺失 PRD / UI_DESIGN / ARCHITECTURE（这三样在工厂侧, 不在仓库里 —— 与事实一致）
```

**C3 产物落盘**：

```
 org/project_analyses.json: 全库 11 条 · 本项目 1 条（PA-0a323a8d）
 ⚠ 但落盘那条是【早期那次】: language=unknown / files=None / stage=None
 ⇒ 理解报告是【实时算】的, 落盘记录不会跟着更新 ⇒ 下次有人读档案会读到过期值（§10 问题 4）
```

## 5. 项目级记忆（D 段，真跑）

| 项 | 读数 |
|---|---|
| D1 条目 | 2 条（1 条 CLI 手工 + 1 条**执行自动写入**） |
| D2 存储 | `~/.factory/project_memory/P-019cc935.json` · 顶层 `['project_id','entries']` · 545 字节 |
| D2 内容 | `[decision\|user_intent] 全量测试: 项目级记忆写入侧已接线…` 来源 cli<br>`[learning\|repo_evidence] [t-3f6f6914] 任务完成（裁定=未表态, 产出证据=repo-changed）` 来源 **exec:EXR-003** |
| D3 注入块 | `inject_block(n=4)` → 167 字符, 格式 `【项目历史记忆】(跨会话…)` + 每条带 `kind\|authority` 标注 |
| D4 第 3 层知识索引 | `knowledge status` → `★ 过期 (3 个文档更新)`；`knowledge reindex` → `✓ 增量: 变更 3 · 删除 0 · 片段 64 {summary:21, raw:43}` |

结论: 四段（存储 / 写入 / 注入 / 索引）都实测通过；写侧由执行事件触发（EXR-003 自动落一条）。

## 6. 8 环链路现状

| 环 | 入口 | 读数 | 判 |
|---|---|---|---|
| 1 会话 | `conversation list` | 3 个会话（本项目 2 个; 1 个含 6 条事实, 理解版本 v6） | ✓ |
| 2 需求 | `conversation facts/prd` | 6 条事实入库（fact-169d75… 等, 带 conversation_id） | ✓ |
| 3 分析（项目理解） | `understand <repo>` | OPERATION 0.90 · medium 83 文件 · node 栈 | ✓ |
| 4 架构设计 | `arch list --project` | `A-d0191f2ea4` 8 节 · CREATED · project=P-019cc935 | ✓ |
| 5 任务拆解 | `tasktree show` | `PLAN-9e32c8b904` confirmed · 12 域/12 叶 · 完成 2 (16.7%) | ✓ |
| 5b 粒度判据 | 同上 | ⚠ 9/12 个叶"可能没拆到位"（带逐条理由 + 修复命令） | ⚠ 判据在工作 |
| 6 执行 | `execution list` | EXR-001 FAILED（超时）· EXR-002/003 SUCCESS · **EXR-004 RUNNING**（在设计核心数据表） | ✓ 在跑 |
| 7 监控 | `metrics` | Executions 4（2 成功/1 失败/1 运行）· **开发任务 12 叶 / 完成 2 / 16%** · Events 127 行 | ✓（部分节见 §10） |
| 8 运维 | `runtime list` / `backup list` | hermes-runtime AVAILABLE · **备份: 暂无**（清场后未重建） | ⚠ 缺备份 |

## 7. 监控/看板三处对账（同一句"任务"）

```
 kanban     共 12 个任务
 task list  12 tasks（台账 0 + 开发 12）
 status     开发任务 2/12 叶（16%）by_status {'completed': 2, 'claimed': 1, 'pending': 9}
 ⇒ 三处一致 ✓（今天修掉的"看板 12 / task list 0"已消）
```

## 8. 端到端真实执行（本次新增 EXR-004）

```
 EXR-001  FAILED   hermes 命令 300s 超时（已修: 默认超时提到 900s）
 EXR-002  SUCCESS  verdict="done"（22 测试 + 契约守卫 + 反向验证）⇒ 叶 completed, 仓库提交 7d156ea
 EXR-003  SUCCESS  verdict="done"（token/me/bind-phone 契约一致, 401/403 分开, 94 测试）⇒ 叶 completed
 EXR-004  FAILED   叶 t-a75b1500 "设计并落地核心数据表…" —— **hermes 命令 900s 超时**
                   （叶已按新语义回 pending + retry=1 + 写明"第 1 次失败 ⇒ 交回重试", 未误判为完成 ✓）
                   ⚠ 两个连带事实见 §10 新增发现 7/8
```
派活链路核查: EXR-004 的 `input` 里含 `task` 与 `instruction`（任务名 + 验收 + 项目仓库）✓ ⇒ 上轮修的"派活没给上下文"确实生效。

## 9. 三入口可用性

```
 CLI     usage: factory [-h] [--root ROOT] [--json]                ✓
 API     http://127.0.0.1:8787/api/status → HTTP 502（服务未启动; 502 来自本机代理, 不是服务)
 WebUI   同上（未启动时表现为 000）
 ⇒ 结论: 三个入口里 CLI 在役; API/WebUI 属"能起但当前没起"（启动命令见 docs/实跑 文档）
```

## 10. 本次发现的问题（按严重度）

| # | 问题 | 证据 | 严重度 |
|---|---|---|---|
| 1 | **执行体不总输出裁定行** ⇒ 完成只能靠产出证据判 | EXR-003 输出 `verdict=未表态`（EXR-002 有 `done`）⇒ 系统按保守路径处理（没当假完成）✓ 但契约遵守率不是 100% | 中 |
| 2 | **metrics 的 Agents 表与 Validation 表全是 0** | Agents 全部 `assignments=0`；Validation `rules=0 runs=0`（`~/.factory/ops/` 为空）⇒ 监控这两段目前是**空壳**（派工/验证的结果没回写） | 中 |
| 3 | **运维环无备份** | `backup list` → 暂无（清场时删了）⇒ 需要重建一次才有兜底 | 中 |
| 4 | **理解产物落盘会过期** | `project_analyses.json` 里本项目记录仍是早期值（language=unknown）而实时报告已变（83 文件/node） | 低 |
| 5 | **存量守卫债** | 架构 9/15 · 分类 3/6 有红（今天清了 ruff 的 47 条, 这两组没动） | 低（债） |
| 6 | **粒度** | 9/12 叶"可能没拆到位"（判据在工作, 未强拆 —— 等你定要不要 `expand --deep`）。★ 收口时得到**实证**（新增发现 7） | 低→**升中** |
| 7 | **粗叶装不进执行窗口（粒度问题的实证后果）** | EXR-004 那个叶（"用户/教练资料/可授课时间段/课程预约/课时包与课时账户/消课记录/通知记录 + ORM + 迁移"七合一）跑满 **900s 仍未完成** ⇒ 超时失败。⇒ 粒度不只是"好不好看"，是**能不能跑完**的问题 | **中** |
| 8 | **超时会留半成品在工作区** | EXR-004 超时后场景仓库留下 **6 个未提交文件**（执行体干到一半被掐）⇒ 下一个叶会踩到这份脏工作区（本仓历史上"前次执行留的 package.json 导致 create_file 不幂等"就是同族） | **中** |

## 11. 方法边界声明（本次【没测】或【测不到】的）

```
 · API/WebUI 的三入口端到端（未启动服务 ⇒ 只测了 CLI 面）
 · 运维环的【真实恢复】（recover）没跑 —— 需要先有备份
 · "停手"路径的真实场景（无真实停手场景；由守卫 + 映射保证, 已在上一份报告注明）
 · 存量债的逐条定性（只报了数字; 未逐条判"该修还是该留"）
 · 派工→Agents 指标回写（§10 问题 2 只做了读数, 未查根因）
 · 本轮执行已收口: EXR-004 FAILED（900s 超时, 见 §7/§8 与 §10 新增发现 7/8）
```
