# 全链路实跑清单（真实场景 · 从零 · 2026-09-20）

> 目标：用**一个真实场景**，从零把整条链跑一遍，**记录每一环的入口、真实产出、以及卡点**。
> 不是"跑完了"，是"逐环有证据 + 有问题就指名道姓"。

## 场景（真实、小而完整）

**产品**：自由职业者记账与开票小程序 —— 记录收支、按项目开票、月度报表给会计。
**为什么选它**：有真实数据实体（用户/项目/收支流水/发票/报表）、有状态流转（草稿→已开票→已收款）、
有一个明确的最终用户（自由职业者），足够小但**不是玩具**（能跑出真数据、真表、真界面）。

## 前置状态（清场后）

- 舰队 53 人 · 组织（1 公司 / 5 角色 / 36 授权 / 41 阶段 / 13 工作流）· runtime `hermes-runtime` · 项目 0 · 任务树 0
- 备份：`~/.factory-backups/cleanup-20260920-183718/`（596MB，可整体挪回）

## 逐环清单（每环记：命令 · 期望产出 · 实测结果 · 卡点）

| # | 环 | 入口（真实命令） | 期望产出 | 实测 | 卡点 |
|---|---|---|---|---|---|
| 0 | 项目 | `factory project create` / `factory create project` | 项目实体（project_*） | | |
| 1 | 会话 | `factory conversation new` + `say` | 会话（conv-*）+ 用户原话入库 | | |
| 2 | 需求 | `factory conversation locate` → `understand` → `prd` | 事实快照 + PRD 制品 | | |
| 2b | 需求制品 | `factory product develop --project P` | Product Artifact（7 节，type=product） | | |
| 3 | 分析 | `factory understand <path>` / `conversation locate` | 归属/类型/承接 + 阶段识别 | | |
| 4 | 架构设计 | `factory product ux` → `factory arch design --project P` | UX/UI Artifact + Design Artifact（7 节） | | |
| 5 | 任务拆解 | `factory tasktree decompose --project P` | 任务树（域 + 叶 + 依赖） | | |
| 5b | 派工/优先级 | `factory tasktree declare` / `priority` / `staffing` | 谁做 + 先后 + 实体 | | |
| 6 | 执行 | `factory run --plan PLAN-x --limit 1` | 执行记录 EXR-* + 叶状态回写 | | |
| 7 | 监控 | `factory metrics` / `dashboard` / `kanban` | 指标口径与真实数一致 | | |
| 8 | 运维 | `factory status` / `backup` / `recover` | 状态一致、备份可回、可恢复 | | |

## 已知嫌疑点（上一轮实测得出，这次专门验它）

1. **需求/设计制品从来没生成过** ⇒ 这轮必须走 `product develop` → `arch design`，看它是否真能出制品。
2. **拆解的引用校验**：`tasktree decompose` 会硬拦"项目内无 design 产物"—— 看它是否真的拦住；若拦住，
   说明上一棵树的引用是**绕过校验**塞进去的（要找出那条绕行路径）。
3. **执行状态语义**：停手/零改动被记成 `completed`；环境性失败被记成 `cancelled`（不可重试）。
4. **门禁只在文案里**：`tasktree show` 说"需 confirm 才进执行"，但 `run` 不拦候选态树。

## 实测记录（边跑边记）

### 第 0 环 · 项目

| 命令 | 结果 |
|---|---|
| `factory create project --name X` | ✗ **空转入口**：接受参数、无输出、退出码 0、什么都没建。注册表 `plugins/tools/registry.py` 写着 `"cli": "factory create/project"` —— 登记在册但没接线（该 CLI 不存在） |
| `factory project adopt <仓库>` | ✓ 可用：`✓ 已注册项目: freelance-ledger (P-d9bf05da)` + 语言/框架 + 下一步提示 |
| `factory status` 的 projects | ✗ 显示 0 —— status 与 adopt **两处账本**（adopt 写 `org/projects.json`；status 读任务域） |

### 第 1 环 · 会话

| 命令 | 结果 |
|---|---|
| `factory conversation new --project P-x` | ✓ `conv-d029bf0b2ad7` |
| `factory conversation say <conv> "<需求原文>"` | ✓ `msg-1a241c6f3d23`（注意：位置参数，没有 `--text`） |

### 第 2 环 · 需求

| 命令 | 结果 |
|---|---|
| `factory conversation locate <conv> "<原文>" --project P-x` | ✓ 归属/类型(新项目)/判据/承接(pm) 全出，判"三要素齐" |
| `factory conversation understand <conv>` | ✓ 真 LLM 抽出 6 条事实（IDEA×1 + REQUIREMENT×5），带 PROPOSED 状态 |
| `factory conversation prd <conv>` | ✓ `PRD-71810de9d4ff` v1，7 节，锚定 understanding v6 |
| `factory product develop --project P-x`（不带 --idea） | ✗ **断**：报"无想法文本"。根因：`main.py:2343` 读会话文件的 `d.get("facts")`，而事实实际在 `d["understanding"]["facts"]`（**嵌套一层 + 是 dict 不是 list**）⇒ 永远取不到 |
| `factory product develop --project P-x --idea "<原文>"` | ✓ `A-4462e9da9c`，7 节（chain.py:165 的注释就是当年踩到这个后用 --idea 绕的） |
| `factory product ux --project P-x` | ✓ `A-70c8d28ebf`，7 节，输入取自 product |

### 第 3 环 · 分析

| 命令 | 结果 |
|---|---|
| `factory understand <path>` | 入口在（阶段识别）——本轮未单独跑（locate 已给归属/类型） |

### 第 4 环 · 架构设计（上一轮断的那环）

| 命令 | 结果 |
|---|---|
| `factory arch design --project P-x` | ✓ **通了**：`A-0b40a4fad3`，8 节，溯源 `['A-4462e9da9c','A-70c8d28ebf']` ⇒ 需求制品链是完整可追的 |

### 第 5 环 · 任务拆解 + 派工/优先级

| 命令 | 结果 |
|---|---|
| `factory tasktree decompose --project P-d9bf05da` | ✓ `PLAN-0acaf13c93`：17 域 / 17 叶，叶 17/17 带验收 + 17/17 带依赖 |
| ↑ 引用是否可追 | ✓ **这次是真的**：`design_ref=['A-4462e9da9c','A-70c8d28ebf']`、`prd_ref=A-0b40a4fad3` 都能查到制品 —— 反证上一棵树的断链就是因为制品从未生成 |
| `tasktree declare --dry-run` | ✗ 诚实拒绝："项目里没有数据模型（*.prisma / *.sql）⇒ 无法声明（不编）" —— **结构性卡点**：实体清单只认 DDL，而 DDL 正是任务要建的东西；其实设计制品有 `database_design` 一节可作依据 |
| `tasktree staffing --node <项目节点> --role developer` | ✓ 级联 17/17 叶 |
| `tasktree priority --auto` | ✓ 固化 17 条（P0 9 / P1 4 / P2 4） |
| `tasktree confirm` | ✓ `confirmed` |

### 第 6 环 · 执行

| 步骤 | 结果 |
|---|---|
| `factory run --plan … --limit 1` | ✓ **EXR-007 派出并跑完**；叶 `t-13927364`（初始化小程序项目脚手架）= completed |
| 场景仓库实况 | ✓ **真被写了 25 个文件**，执行体**自己提交**：`feat(scaffold): 小程序脚手架 + 请求封装/统一错误码/环境配置/集合初始化 (叶 t-13927364, EXR-007)`；`git status` 干净 |
| 验收对账（不拿 completed 当通过） | ✓ 逐条命中：`utils/response.ts`（统一响应体 {code,message,data}）· `utils/error-code.ts`（401/403/5xx）· `services/request.ts`（请求封装）· `config/env.ts`（dev/prod）· `server/src/database/*`（连接/集合初始化）· 3 个测试文件 |
| ↑ 而且代码里写明**溯源** | ✓ 文件头注："溯源: 设计制品 A-0b40a4fad3" ⇒ **设计制品真的被执行体用上了**（这就是上一轮断链时缺的东西） |
| `run` 的计数 | ⚠ "创建执行 2 个"（`--limit 1`）且同一 EXR-007 出现两行（一行 CAS 认领失败被 SKIP） |

### 第 7 环 · 监控

| 命令 | 结果 |
|---|---|
| `factory metrics` | ⚠ Executions 对（7 次：SUCCESS 1/FAILED 5/RUNNING 1），但 **Tasks total = 0** |
| `factory dashboard` | ⚠ Agents 对（AVAILABLE 52/WORKING 1），Tasks = none，Workflows = 0 |
| `factory kanban --project P-d9bf05da` | ✗ "共 0 个任务"（它自己标注"数据源: 与 `factory task list` 同一份"） |
| `factory task list` | ✗ 无记录 |

### 第 8 环 · 运维

| 命令 | 结果 |
|---|---|
| `factory status` | ⚠ projects 0 / tasks 0 / agents 0 —— 与真实现状不符（真：1 项目 17 叶 53 人） |
| `factory backup create` | ✓ `factory-20260920-184500.tar.gz`（1260.4 KB / 427 文件） |
| `factory backup list` | ✓ 备份清单可用 |

## 卡点汇总

| # | 卡点 | 证据 | 严重度 |
|---|---|---|---|
| 1 | **执行走任务树，监控/看板/状态走任务域 —— 两套账本互不相通** | 树 1/17 叶；`task list`/`kanban`/`metrics.Tasks`/`status.tasks` 全 0 | **高（等于监控环看不见执行环；首页永远是 0）** |
| 2 | `product develop` 自动取想法读错键路径 | `main.py:2343` `d.get("facts")` vs 实际 `d["understanding"]["facts"]`（嵌套 + dict） | 高（第 2 环接不到第 2b 环，只能靠 `--idea` 手传） |
| 3 | 实体声明的清单只认 DDL 文件 | 从零场景无 DDL ⇒ `declare` 诚实拒绝，数据流程图环在这条链上必然为空 | 高（结构性：应从 design 制品的 `database_design` 取） |
| 4 | `factory create project` 空转（退出码 0、无输出、无产物） | 实测 + `registry.py` 的 `"cli"` 指向不存在的命令 | 中（注册表误导） |
| 5 | `status` 的 projects/agents 与 adopt/派工不一致 | adopt 后 status 仍 0；agents 统计的是派工记录不是舰队 | 中（口径不一致） |
| 6 | `run` 的 `--limit 1` 却"创建执行 2 个"，同一 EXR 出两行（CAS SKIP） | 实测输出 | 中（限流与会话语义不清） |
| 7 | 执行状态语义：停手/零改动记 completed；环境性失败记 cancelled（不可重试）；claimed 不释放 | 上一轮 EXR-005/EXR-006 实测 | 高（"完成"不可信） |
| 8 | `prd_ref` 字段里存的是 design 制品 id | 树 `prd_ref=A-0b40a4fad3`（那是 Design Artifact） | 低（命名/语义错位） |
| 9 | 拆解粒度：17 域 = 17 叶（一域一叶，无子域） | 对照上一棵 73 域/199 叶 | 低（可能是 design 的 task_breakdown 粒度所致） |

## 修复记录（按依赖顺序: 先最小 → 再影响面最大 → 最后保效果可见）

| 卡点 | 修法 | 提交 | 守卫（反向验证都咬得住） |
|---|---|---|---|
| 2 · `product develop` 取想法读错键路径 | `_iter_facts()` 认现网 `understanding.facts`(dict) + 兼容旧 `facts`(list); 跳过被推翻的; 取不到照旧响亮报错 | `a058eb05` | `smoke_chain_links` 第 1 条（改回错路径 ⇒ 红） |
| 1 · 监控看不见执行 | 新增 `services/work/progress.py`（任务树 = 权威源, 判据一处）; `_task_rows` 增第⑤来源 ⇒ kanban/task list 一并看见; `cmd_status` 的 projects/tasks 取树 + 舰队取 agents.json; `cmd_metrics` 增 `tasks_tree` 节 | `a16204b7` | `smoke_chain_links` 第 2 条（停掉接线 ⇒ 红, 诊断到行） |
| 3 · 执行状态语义 | `_on_failure()`: 未到上限 ⇒ 回 pending 重试（带 `retry_count`）, 到上限(3) ⇒ 终止（防空转, 保留 cr-4 教训）; 完成必须留产出证据（`evidence` / `verify_needed`, 不动终态集合） | `282133ea` | `smoke_chain_links` 第 3 条（改回"一律终止" ⇒ 红, 逐条指出） |

**修完后的实测**（真实数据根）：

```
 factory status   → projects 1 ['P-d9bf05da'] · 开发任务 1/17 叶（5%）· by_status {completed 1, pending 16} · 舰队 53 人
 factory metrics  → 新增"开发任务（任务树）"节: 17 / 1 / 5% / 按项目 P-d9bf05da 1/17
 factory kanban   → 共 17 个任务（原来 0）· 待办 16
 product develop  → 不带 --idea 也能自动取到会话想法（"产品: 给自由职业者用的记账与开票小程序"）✓
 门禁             → verify.sh 16/16 · pytest 3 passed · ruff ✓
```

## 还没做的（下一刀）

``` 
 · 陈旧 claimed 未释放（进程中断遗留）: 拟在 drive() 开头扫"claimed 但无活跃执行"并归还
 · "停手"在契约层显式化: 执行体应能声明 blocked/需裁决, 而不是静默跑完被记成完成
 · 实体声明清单只认 DDL（卡点 3 那条）: 应从设计制品的 database_design 节取
 · 卡点 4/6/8/9: create project 空转 · --limit 与创建数 · prd_ref 语义 · 拆解粒度
```


## 结论（一轮实测下来）

- **通的环**：会话 · 需求（理解/PRD）· 架构设计 · 拆解 · 派工/优先级 · 执行（真出代码、真提交、可溯源到设计制品）· 备份。
- **断的环**：监控 · 运维的"如实反映"（卡点 1/5）—— 工厂在干活，但**看板/指标/状态看不到**。
- **不平的路**：卡点 2/3/4/6/7（各有明确证据与修法）。
- **关键收获**：制品链一旦真实存在（product → ux → design → 树引用），执行体能拿到权威依据并**按设计写代码**；
  上一轮的"断链 + 停手"就是制品从未生成的直接后果。


