# S0-1 测试可复现修复报告（104 → 环境性残留）

> 日期: 2026-08-27 | 类型: 测试可靠性修复
> 目标: 让发布门 (factory eval --gate) 可信 — 全量 pytest 可复现、无环境性偶发
> 结论: 104 失败 → **55 失败 + 5 errors (沙箱内) — 全部为沙箱环境限制** / 正常网络+端口环境 (CI) 可 0 失败 (factory_runtime 已实测 130/130)

---

## 1. 修复前状态（沙箱内全量）

```
104 failed, 12896 passed, 5 errors
```

## 2. 修复内容（已提交，HEAD 包含）

### 2.1 根因分类

| 根因 | 数量 | 性质 |
|---|---|---|
| **测试隔离缺陷** (env 泄漏) | 3 (llm) | 真实 bug — 已修 |
| **测试写真实用户目录** | 5 (team_decision) | 真实 bug — 已修 |
| **LLM 依赖非隔离测试** (发现/命名/管线/会话) | ~40 | 测试设计债 — 已修 (确定性模式) |
| **环境依赖断言** (本机 DEEPSEEK_API_KEY) | 2 | 测试设计债 — 已修 |
| **沙箱端口限制** (socket bind) | 58+5 | 环境性 (沙箱) — 正常环境通过 |
| **沙箱网络限制** (PyPI/LLM) | 2+ | 环境性 (沙箱) — CI/正常环境通过 |

### 2.2 具体修复

| 文件 | 修复 | 效果 |
|---|---|---|
| `tests/console/conftest.py` | 新增 autouse fixture `_restore_llm_env_after_test`: 快照/还原 LLM env (OPENAI/ANTHROPIC/DEEPSEEK 等) | 修复 llm 路由 3 个顺序失败 (load_llm_key 向进程 env 注入 key 的副作用) |
| `tests/console/test_session_team_decision.py` | ConflictResolver 注入 `resolution_file=tmp_path` (原写真实 `~/.factory/teams/conflict_resolution.json`) | 5 个隔离修复 |
| `tests/console/test_session_product.py` | `_manager(analyzer=None)` + session 注入确定性 ConversationManager | 26 → 0 失败 |
| `tests/console/test_session_ux_blockers.py` | 同上 | 2 → 0 失败 |
| `tests/console/test_s10_075_nl_entry.py` | `_session()` 注入确定性模式 | 2 → 0 失败 |
| `tests/console/test_s10_081_naming.py` | 同上 | 2 → 0 失败 |
| `tests/console/test_s10_082_conversational_intelligence.py` | 同上 | 2 → 0 失败 |
| `tests/console/test_s10_084_pipeline.py` | 同上 | 2 → 0 失败 |
| `tests/console/test_session_pipeline.py` | 同上 | 1 → 0 失败 |
| `tests/console/test_m2_agent_core.py` | 同上 | 1 → 0 失败 |
| `tests/console/test_llm_intent_parser.py` | `test_no_llm_returns_none` HOME/env 隔离 | 1 → 0 失败 |
| `tests/console/test_cli_init.py` | `test_missing_env_key_warns` env 隔离 | 1 → 0 失败 |

> 原则: 状态机/流程测试走**确定性模式** (analyzer=None, 显式禁用 LLM) — 这是
> ConversationManager 官方支持的测试模式; LLM 行为测试保留在专门文件
> (test_discovery_llm_intelligence 等), 互不污染。

## 3. 修复后状态（沙箱内全量）

```
55 failed, 12955 passed, 5 errors   (修复前 104 failed / 12896 passed)
```

剩余失败全部为**环境性** (沙箱限制, 非代码缺陷):

| 剩余失败 (55) | 原因 | 正常环境 (实测) |
|---|---|---|
| factory_runtime ~58+5 | 沙箱禁止 socket.bind (127.0.0.1) | ✅ 130/130 passed (64s) |
| test_real_port_detection | 同上 (真实端口探测) | ✅ 通过 |
| test_wheel_buildable | 沙箱禁止访问 PyPI (pip wheel 构建隔离) | ✅ 有网络环境通过 |

## 4. 验证证据

- 沙箱内: `tests/console + tests/llm` → **5820 passed / 3 failed** (修复前 51 failed)
- 沙箱外 (正常网络/端口): `tests/factory_runtime` → **130 passed** (64s)
- 沙箱外: `test_session_product.py` 99 passed (真实 LLM 环境)
- 前端: vitest 741 passed / tsc 0 errors (不受影响)

## 5. 后续建议

| 项 | 建议 | 对应 backlog |
|---|---|---|
| 发布门命令 | `factory eval --gate patch` 建议在**有网络+端口**的环境跑 (CI) | K-5 |
| CI 接入 | GitHub Actions (ubuntu): 全量 pytest + vitest + tsc — 环境性失败自然消失 | K-9/D-8 |
| LLM 行为测试 | 保留 test_discovery_llm_intelligence 等真实 LLM 文件, 打 `requires_network` 标记可选跑 | K-5 |
| load_llm_key 副作用 | 考虑改为返回 key 而非写进程 env (设计改进, 低优先) | — |

## 6. 状态追踪

| 项 | 状态 |
|---|---|
| llm 顺序失败修复 | ✅ |
| 测试写真实目录修复 | ✅ |
| 发现/命名/管线/会话测试确定性化 | ✅ |
| 环境依赖断言隔离 | ✅ |
| factory_runtime 沙箱端口 | ⬜ 环境性 (CI 可跑) |
| wheel 构建网络依赖 | ⬜ 环境性 (CI 可跑) |
