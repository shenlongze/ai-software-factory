# 审计 域（api/domains/audit）

| 项 | 值 |
|---|---|
| 路径前缀 | `/api/audit` |
| OpenAPI tag | `审计` |
| 端点数 | 0（骨架，待刀2/刀3 接线） |
| 本域实况 | audit_store；CLI 侧当前只是只读骨架，待补 ✗ |

> 接口说明由 `docs/api/openapi.json`（单一事实源）生成，**禁手写** ✓
> 生成脚本在刀5 落地；当前为占位 ✓
