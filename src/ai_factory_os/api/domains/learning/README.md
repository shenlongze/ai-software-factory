# 学习 域（api/domains/learning）

| 项 | 值 |
|---|---|
| 路径前缀 | `/api/learning` |
| OpenAPI tag | `学习` |
| 端点数 | 0（骨架，待接线） |
| 本域实况 | **2026-09-15 补入** —— 契约域(12)与服务域(9)都有 learning，产品核心「学习自治」也是头等事，唯独 API 面漏了 |
| 定位 | 主链第 8 环「经验回流」：执行/验收的产物沉淀成经验，反过来影响后续选择（agent / provider / skill / workflow / project / decision） |
| 老区对应 | `intelligence`(7) · `learning`(5) · `experiences`(4) · `promotions`(7) · `recommendations`(2) · `selection`(1) · `optimization`(23) · `experiments`(11) 等 —— 端点搬迁待排 |

> 接口说明由 `docs/api/openapi.json`（单一事实源）生成，**禁手写** ✓
> 生成脚本在刀5 落地；当前为占位 ✓
